# Deep read — nine chapters, nine readers, consolidated

**All nine reports are in.** Each reader read one chapter whole and twice, then read it against
all eight others, per `DEEP_READ_BRIEF_2026-08-11.md`. **Every finding recorded below was
verified against the files by hand before it was written down.** Findings that did not survive
that check are not here.

**Scale:** ~30 blocking, ~90 real, ~60 thin. **This document carries the blocking set and the
real findings that change what a reader gets.** The per-chapter tails live in the reports.

---

## 1 · What the fan-out found that nine separate reads would not

**Corroboration.** Nine readers, none able to see another's report, and the important findings
came back from more than one direction:

| finding | independently found by |
|---|---|
| `APPENDIX_B` names the wrong daemon for five of seven quests | **ch2 · ch3 · ch5 · ch7** |
| `the spiral` survives as a name for the Form | **ch1 · ch3 · ch4** |
| `Fixer/Healer` vs `Fixer-Healer` | **ch2 · ch5 · ch6 · ch7** |
| ch8 has no Appendix F pointer | **ch4 · ch6 · ch8** |
| `APPENDIX_F` lists four pairs where six chapters draw one | **ch3 · ch4** |
| **ch4's and ch5's exiles contradict** | **ch4 and ch5, from opposite sides** |
| `ch8:272`'s `Red` | **ch2 · ch8** |

**The exile pair is the strongest signal available in this design.** Two readers, each holding
one chapter, each reporting the same collision as their own chapter's problem. Neither could
have been primed by the other.

**And the brief was wrong about the form, which four readers caught.** It listed
`5 Journey to the Center` as part of the six-Face shape. That is **ch2's and ch9's** heading;
all six Face chapters use `Section 5: The <Daemon>, Up Close`. ch1, ch3, ch5 and ch8's readers
each corrected it independently rather than deferring. **A brief that gets deferred to is a
brief that produced nine echoes.**

## 2 · Three of the eleventh-hour edits are implicated

**Everything in this section was written on 2026-08-11 and every defect in it is mine.**

**a · `ch3:286` contradicts `ch3:300`, and the amendment cited `:300` as evidence for `:286`.**

> **`:286`, mine** — *"**Nobody graduates out of it.** The beginner runs the form and the master
> runs the form."*
> **`:300`, Maera, fourteen lines below** — *"**For your first year, run two:** notice, and say.
> The other three are what you do afterwards… students who learn two never come back for the
> other three."*

`AMENDMENT_FORM_METAPHOR_2026-08-11.md` §2 lists that exact block as support — *"a sensei in
all but name."* **I quoted, as evidence for a frame, the passage my new sentence denies.** A
form is the one thing you cannot run two-fifths of. Either the margin hand is wrong in a way
the chapter must mark, or `:286` overstates. **It is the newer prose.**

**b · `ch9:388` contradicts the last sentence of ch8 and the enrollment page.**

> **`ch8:904`, the final line of the chapter before** — *"You've carried the whole map this
> far. **You don't need anyone reading it over your shoulder anymore.**"*
> **`ch9:388`, mine** — *"**It takes another pair of eyes** to tell you what you do with them."*
> **`enrollment.md`** — *"**None of it is required. You can run the rest alone**… and people do,
> and it takes longer."*

**And the amendment I wrote to justify it says:** *"pitched to match `enrollment.md`'s own
honesty… rather than overselling the mat. **The book spent nine chapters teaching a reader to
spot a sales move; the close cannot make one.**" The landed sentence overshoots the brief it
was written to, and the tell is exact: **it shifts from ranking to necessity in the final
clause.** `ch9:157` gets the identical claim right — *"finds it faster than you will by
yourself… catching yourself at that alone is **the hard part**"* — which ranks.

**It also joined a pattern.** `fastest`/`faster` appears **five times in ch9 and every one is
inside a pitch**, against `ch9:618`, the Recap's own opening: *"You wanted an answer; **this is
better and slower**."* Mine made it five.

**c · `ch2:569`'s Appendix B on-ramp routes the reader into a page that tells her she is
finished, and into five wrong daemons.**

> **My pointer** — *"Open the page **now** rather than at the end… a quest you save for after
> the last page becomes **homework**."*
> **`APPENDIX_B`, first line the reader sees** — *"You crossed the forest. You met the Faces.
> You know the moves… What follows isn't **homework** and it isn't a recap."*

The pointer's facts check out — eight quests, Quest 1 is ch2's, seven days. **The appendix's
opening is what must move.** And Appendix B had been unreferenced from the manuscript, which
is the only reason its daemon column survived; **the on-ramp is what made it reachable.**

## 3 · Blocking — verified, no ruling needed

| # | site | the defect |
|---|---|---|
| **1** | `ch8:272` | *"therefore they're **at Red**"* — **the only stage-colour in the manuscript.** `DECISION_LOG` A4 records the cut as APPLIED 2026-08-01, *"0 occurrences… anywhere in `manuscript/`"*, and warns *"a ruled cut the ledger reports as done is worse than one never ruled."* Undefined for the Green reader; for the savvy reader it completes the mapping one paragraph after `ch8:266` names the answer-set. |
| **2** | `ch7` ×23 | **Production tags shipping in prose.** `[TRANSLATE]` ×6, `[CONTROL]` ×7, `[DISSATISFACTION → SATISFACTION]` ×10. **Every other chapter reads 0.** `SPEC_BRACKET_TAGS_2026-07-29.md` §6 D1 ruled the strip and called it a blocker: *"a production tag in shipped prose is a typesetting artifact with a reader in front of it."* ch8's five were stripped; ch7's were not, so a two-chapter artifact is now a one-chapter anomaly. |
| **3** | `ch7:705` vs `:606` | **Two shipping sentences send the reader to different sections for the same exercise.** `:606` *"the Care ↔ Impact axis in **Section 4**"*; `:705` *"the axis you drew in **Section 3**"*. `Draw the Axis` is at `:215`; Section 4 opens at `:204`. The restructure updated one and missed the other. `:705` is inside the move ch7 calls *"the one the whole chapter has been building toward."* |
| **4** | `ch6:268` | **`Draw the Axis` draws no axis.** ch6 has **one** `Try this now.` where ch3, ch4, ch5, ch7 and ch8 have two — no axis figure, no integrated-sentence template, no settle. `ch6:456` then opens *"**You drew** the Structure ↔ Agency axis in Section 4."* The shared instrument was rebuilt in the Architect's own material, which is constraint 4 failing inward. |
| **5** | `ch4:651` vs `ch9:654` | **The only worked example of Name the Unnameable fires late.** *"**Forty seconds later** you say…"* against ch9's *"its entire value is that it happens **inside the four seconds** rather than in the hallway"* — and against ch4's own test eighteen lines down, *"in the moment."* |
| **6** | `APPENDIX_F` | **Names four pairs; six chapters draw one.** *"I name four because four chapters needed them."* Two of the four are filed under **Chapter 3** and **neither is ch3's pair** — they are ch3:640's throwaway examples. **Feeling ↔ Function is absent**, as are Force ↔ Restraint, Structure ↔ Agency and Which Game ↔ Which Altitude. Five chapters point readers there for *"additional pairs."* |
| **7** | `APPENDIX_B` | **Wrong daemon for Ch3, Ch5, Ch6, Ch7, Ch8.** Only Ch4 correct. Also `:100` renders **Close with Honest Terms** as a Bridge-Builder instruction — one name, two practices, the `WAVE` class exactly. |
| **8** | `ch7:63` | **Section 1 is titled *The Exile* and contains no exile.** No Diplomat character, no departure, nothing lost. `ch7:90` then opens *"**After the Diplomat left**…"*. ch3, ch4, ch6 and ch8 all carry the *"never said 'go'… amounts to exile"* formula; ch5 has its own. **23 lines against 35/50/57/83/84** — shortest by a third, in the second-longest chapter. |
| **9** | `ch9:169`, `:682` | **The reader is told she owns the deck.** *"you have been holding all of it since Chapter 3"* and *"It costs you nothing"* — four lines before *"it begins the moment you open the box."* All six Face chapters ship the identical italic line: *"Your twenty **live in the deck rather than on this page**."* |
| **10** | `ch3:628`, `:661`, `:1005` | **Promised three times, broken once.** *"3-2-1 — here, and in **every chapter after this one**"* and *"Every chapter after this one draws its own pair."* ch4–ch8 carry two 3-2-1 blocks each. **ch9 has zero, and no polarity pair.** |
| **11** | `ch9:662` vs `ch7:620` | **ch9 defines the Connector as what ch7 rules out.** *"the only person both sides will still talk to: the Connector's **actual** job"* against *"the word usually means something else. This chapter does not train contact… It trains connection that has survived being told the truth."* Inside the Last Rep, in the read that tells the reader her default Face is right. |
| **12** | `ch3:189` vs `:624` | ***emotional processing* names the distortion, then the practice.** `:189` *"In emotional processing, the emotion is the problem"*; `:624`, after a **Try this now**, *"**This is emotional processing**, and the material it works on has weight."* The `:624` sentence is doing safety work and should stay. The noun is the collision. |
| **13** | `ch9:344` | **A triple negative that inverts.** *"you don't think deep introspection and shadow work… **isn't** fun?"* means *you think it is fun* — the opposite of the indignant question the passage needs. Distinct from the two ruled testimony sites four lines below. |
| **14** | glossary | **No `Architect, the`. No `Controller, the`.** One of six Faces and the daemon ch3 works up close. Both are named in ch9. |
| **15** | glossary `:93` | **`Player, the` defines the Vulnerable Child**, locator `Ch 2 §6, §7` — **no Chapter 9 locator at all**, in the entry for a chapter titled **THE PLAYER**. `index.md:13` bolds ch2 as primary. Note ch1 and ch2 use only lowercase *player* for that sense; **the capitalisation is introduced by the apparatus.** |
| **16** | `ch9:388` | See §2b. |
| **17** | `ch3:286` | See §2a. |
| **18** | `ch2:569` | See §2c. |

## 4 · Blocking — needs a ruling

**These are contradictions between two defensible pieces of writing. Each needs a decision
before anything moves.**

**R1 · Which exile is canon.** ch4 and ch5 tell incompatible versions of the same event.

> **`ch4:80`** — *"**The villagers chose the Regent.** Not because the Challenger was wrong.
> Because the Challenger was expensive."* → the Regent is present, is the rival, and causes the
> exile.
> **`ch5:74`** — *"**After the Challenger left**… the Regent showed up at exactly that moment."*
> → the Regent arrives afterward, into a vacuum, as rescuer.

**ch3 sides with ch4.** ch5's version additionally characterises the Challenger as having *"no
interest in guarding them"* and having *"moved on to the next fight"* — **the caricature ch4:450
spends a section dismantling.** And it removes the Regent's own complicity from the chapter
whose subject is what a keeper enters in the record.

**R2 · *shadow* or *distortion*.** `ch1:254` defines **shadow** as the overplayed Face. `ch2:53`
and `ch8:133` both stop the book to instruct otherwise — *"One word each: Shadow, distortion,
cost. **They are not the same, and the work is to tell them apart**."* `glossary:117` prints
ch1's sense and stamps it *Ch 1*; `index.md:33` files it under **The daemons**. **Three
surfaces, three answers.**

**R3 · Seven daemons or eight.** `ch2:275` *"**Seven** daemons live in the Forest, and the player
at its center is you"* against `APPENDIX_A:174` *"**Eight** daemons across four domains makes
thirty-two cells"* and `:165` *"the other seven stand behind it."* `ch9:476` states the
reconciliation nothing else carries: *"Seven of them and the child at the center: eight in
all."* `STYLE_SHEET` §6 sides with Appendix A; the glossary with ch2.

**R4 · `Fixer/Healer` or `Fixer-Healer`.** `STYLE_SHEET` §4 rules the slash. **The book runs
hyphen 16, slash 12** — ch5 holds 13 hyphens including its Section 5 heading, ch7 and ch8 one
each; slash owns ch2, ch9, the glossary, the index and three appendices. **The sheet is
currently wrong about what the book does**, and ch6:372 introduces a third form (bare *Fixer*)
inside the fixed Forest litany.

**R5 · `ch5`'s two stage orders.** `ch5:384` — `Inherit → Honor → **Steward → Reform** →
Entrust`. The game moves ship **Move 3 Reform, Move 4 Keep the Vows** — swapped. `ch5:394`
builds a paragraph on why the order matters: *"stewardship earns you loyalty, and **reform
spends it**."*

**R6 · The units.** ch3 says **stages** (28×, including its section heading and all five
subheads). ch5 says **movements** (3×). ch9 says **moves** (6×) and **stages** once, at `:103`.
ch4 says both. **Majority is *moves*, which is also the name.** Deliberately untouched; still
unruled.

**R7 · The commercial surface.** ch9 carries **three near-duplicate coaching pitches**
(`:155–159`, `:192`, `:684`), the *"work I most want to hand off"* sentence twice 525 lines
apart, five speed claims in a chapter whose Recap says *"better and slower"*, a Recap that says
*"not a method but a path"* fifty-eight lines before selling the method, and a closing section
headed *"Two paths exist on the other side of this book"* that **names two purchases and neither
of the two free things the book already contains** — Appendix B's Quest 8, which is ch9's own,
and Appendix H. Each is small. **Together they are the sales move the book taught the reader to
spot.**

## 5 · Real — the ones that change what a reader gets

- **`the spiral` survives ×5** — `ch3:295`, `:814`, `:958`, `:976`, `ch4:829`. The `WAVE-Spiral`
  sweep took the compound and left the bare noun. `ch3:976` *"a full pass of the spiral"*
  collides with the unit ruled 2026-08-11.
- **`EA` is a section heading in ch4–ch8 and ch3 uses it zero times**, never expands it. The
  only expansion left is the retired Key Terms glossary. `ch7:207` compounds it: a vocabulary
  note naming three move types the section does not use (`[TRANSLATE]` has no counterpart at
  all), pointing at a chapter that does not teach them.
- **The Five-Move Form is never credited where the reader is sent.** `ch3:258` promises
  *"Appendix G says where to read each one."* **Appendix G has no Wilber entry for the Form** —
  only the six-Face arc and Laloux. The *Finding Radical Wholeness* credit lives on the
  copyright page and in the glossary. Same failure as the jazz ruling: **ratified, never
  written.**
- **`Neutral Channel` ×11, all in ch7**, a second referent for a word ch3 owns. `SPEC_BRACKET_TAGS`
  D4 ruled its removal **6–0 and it was not applied.** Two surviving `channel`s at `ch7:460` and
  `:480` mean the Diplomat's mode, forty lines from `ch7:494`'s *"the Fire channel"* meaning
  ch3's element.
- **ch7's five stages and five modes are the same five things** — the only Face chapter where
  that is true, and the only one with no Stage Sequence heading. `ch7:883` performs the collapse
  in its own recap sentence.
- **`ch8:648`** — *"it trains **two capacities** rather than one"* against `ch2:362` *"Each one
  ahead trains **a single** superpower."* And ch8 delivers its second as a gerund (*Coaching*)
  where the other five give a role-noun (Alchemist, Disruptor, Storyteller, Strategist,
  Connector), so `ch9:670`'s instruction to write down *"the superpower its chapter named"*
  cannot be answered in ch8's shape.
- **`ch8:240`** — a heading reading *"The Ladder Underneath: What the Sage Knows That the Other
  Five Cannot See"*, against `ch8:232`, `:254`, `:469`, `:531` and `ch9:620`, all of which deny
  exactly that. The only affirmative use of *ladder* about the Faces in the manuscript.
- **`ch9:284`** runs the Form as **six** beats — *"Then you come back"* — and `:286` says *"**You
  know that** the return is not optional."* No chapter teaches a come-back beat; ch3 stops at
  Show Up.
- **`ch5:386`** shrinks the Form and gives it an owner — *"Unlike the Shaman's Five-Move Form,
  fast enough to happen in minutes"* — against `ch3:288` *"ten seconds or ten hours"* and
  `ch9:386` *"**The Form doesn't change size**."* ch8:477 solves the same problem correctly:
  *"The full loop takes a moment or a year… The sequence stays the same."*
- **`ch1:167`** promises *"Every chapter with a Face in it closes by asking where the work
  lands."* What they close with is the domain grid — *"across is where it lands"* — which means
  which of four domains, not who collected. The prize-counter/owners argument appears in ch1
  and **nowhere else in nine chapters.**
- **Three more British spellings in body prose**, none in `copyedit.py`'s word list: `ch8:764`
  *fifty per cent*, `ch6:535` *programme* (ch6 spells it *program* at `:141`), `ch6:217`
  *rigour* (ch6 spells it *rigor* three times).
- **`ch4:275`** — *"The Shaman **(Ch3)** taught you"*, the only short-form chapter reference in
  the body of the entire book. `STYLE_SHEET` §8 rules it out explicitly.
- **`ch1:157` vs `ch9:528`** — *encouragement* is ch1's better word with a test attached and
  ch9's *"least useful response a human being can hand you."* Only two uses in the manuscript.
- **`ch2:213` vs `ch9:358`** — ch2 says the book *"was written from that anger"* and calls it
  the alchemy working; ch9 says *"**The spite did not produce it.**"* Same artifact. **The
  distinction the arc needs — alchemized anger produced the book, unalchemized spite produced
  only the failed course — is stated in neither.**
- **`back_matter/index.md` — no game move from ch5, ch6, ch7 or ch8 is indexed.** `index_build.py`
  hard-codes ten, all from ch3 and ch4. `ch9:662` sends the reader after three of ch7's by name.
- **Repetition that `dupes.py` structurally cannot see**, because a pronoun or clause changed:
  ch6's wellness hour (body + margin), ch6's *"nobody has ever needed courage to say the system
  did it"* ×2, ch4's committee joke (body + margin, four beats in order), ch4's
  hazard/furniture sentence ×2 and its weather image ×2, ch5's three-crises passage ×2, ch5's
  Head's note ×2, ch8's Thalen paragraph ×2, ch7's verbatim 24-word self-repeat at `:46`/`:139`,
  ch1's Diplomat/Challenger shadow examples ×2 in twenty lines.

## 6 · What the readers verified clean

**Worth as much as the findings, because it is what the pass does not have to revisit.**

- **The Last Rep's ten named moves all exist in their named chapters under exactly those
  names** and do what ch9 says — including reproducing ch5's own internal *"Moving it is Move
  3"* sequencing. Chapter numbers all correct. *Six Faces and thirty moves* checks: 6 × 5.
- **All seventeen `WAVE`→`Form` conversions read naturally, no orphaned verbs**, and zero
  `WAVE` or `WAVE-Spiral` survives on any shipping surface.
- **The character-sheet chain is unbroken, 8 of 8** — ch2 → ch3 → ch4 → ch5 → ch6 → ch7 → ch8 →
  ch9, each row naming the one before it, and Appendix H's thirteen fields match.
- **The five channels and five satisfied states are identical across all six Face chapters**,
  the glossary, the index and Appendix C — zero drift in the channel→state mapping, and all
  five capability phrases (*I can explore / rest / act / participate / connect*) match ch3
  exactly at every site. **The book's tightest cross-chapter agreement.**
- **The native-material ladder holds in both directions** — emotion · will · loyalty · logic ·
  relationship · perspective, each claimed once, no chapter borrowing another's.
- **The school prerequisite chain is correct in both directions** across ch4→ch5→ch6→ch7→ch8.
- **The treatise apparatus matches `ch2:578`'s promise** — six treatises, signatures at the
  close of Section 3, numbered first through sixth with no gaps.
- **No Orange substrate leaks out of ch6.** Every optimisation/leverage/efficiency site in the
  other eight is the village's error or another Face's distortion, checked individually.
- **The ladder does not leak anywhere except `ch8:272` and `ch8:240`.** `ch6:305`'s *"two
  altitudes"* was checked and cleared — the book says more, plainly, in ch4, ch7, ch8 and the
  glossary. `ch7:107`'s *pluralistic* is flagged as the closest approach and not called a leak.
- **ch7 does not go easy on its own reader** — measured at or above parity with all five
  siblings on `Try this now` blocks, Examples, tests and cost clauses, and it carries **the most
  cost clauses in the book.** The one soft spot is Section 1, which is the same defect as §3.8.
- **ch8 does not contradict ch9's *"Sage is a seat you take and leave"*.**

## 7 · What this says about the instruments

**Every finding above was invisible to twelve clean steps.** The gaps, named:

1. **Nothing asks whether a concept has one name** — the gap that produced `WAVE`, and it also
   produced `the spiral`, `Player`, `shadow`, `encouragement`, `Fixer/Healer` and `EA`.
2. **Nothing asks whether a ratified ruling was ever written** — the jazz metaphor, `Appendix G`'s
   missing Wilber entry, `SPEC_BRACKET_TAGS` D4's 6–0 ruling, `DECISION_LOG` A4's `Red`.
3. **`dupes.py` needs surface overlap.** Nine repeats above survive it by one changed pronoun.
4. **`xref.py` checks that a pointer resolves, not that it is true.** `ch7:705` resolves.
   `ch3:258`'s *"Appendix G says where to read each one"* resolves. `APPENDIX_F:63` resolves.
5. **`copyedit.py`'s `BRITISH` dict is only as good as its words.** *rigour*, *programme* and
   *per cent* are not in it. **Third round of this lesson.**
6. **`index_build.py` hard-codes its term list**, so four chapters' game moves are unindexable
   by construction.

## 8 · The order the work wants

1. **The rulings in §4 first** — R1 through R7. Six of the eighteen blocking items cannot be
   touched until those are settled, and R1 decides which of two chapters gets rewritten.
2. **The mechanical blocking set** — `ch8:272`, ch7's 23 tags, `ch7:705`, the Appendix B daemon
   column, the Appendix F pair list, the two missing glossary entries, `ch9:344`.
3. **The prose blocking set** — ch7's Section 1 exile, ch6's axis, ch4's forty seconds,
   ch9's deck claim, and the three of mine in §2.
4. **Then §5**, which is where the tail of real findings lives.

**Nothing in §2 should wait.** Those three shipped this morning and two of them contradict
sentences the same documents were written to protect.
