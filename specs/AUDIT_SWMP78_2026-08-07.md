# What is left on `mtgoa-manuscript-changes-swmp78`

**2026-08-07.** Wendell: *"let's check the old and messy one for if there's anything
useful there."* 42 commits, forked **2026-08-01** at `ee18459`, idle since. Its prose
diff is **+265 / −252 across 16 files** — the near-balance that says rewrite rather than
addition.

**A trial merge conflicts in seventeen files, including all eight chapters ch2–ch9.**
That is the cost of taking it whole, and nothing on it is worth that. **The finding is
that three items are worth taking, and all three can be taken directly without merging
anything.**

---

## 1 · `Gate` never got retired in the back of the book — and this is a live defect now

The branch's commit `5700bd6` **Retire "gate" as a name for a daemon** never landed.

**Measured on HEAD today:** the manuscript says **`daemon` 54 times**. The apparatus
still says `Gate` **23 times**, and every one of them names a daemon:

| where | count | sample |
|---|---|---|
| `APPENDIX_B` quest headers | **7** | *Chapter 5 · **Gate**: Protector · Domain: Skillful Organizing* |
| `APPENDIX_B` campaign headers | **4** | *Domain: Direct Action · **Gates**: Fixer/Healer + Damaged Self* |
| `APPENDIX_B` running prose | **9** | *name which **gate** was running in you* · *your two most-traveled **gates*** · *Which **gate** grew?* |
| `APPENDIX_E` | 2 | *which **gate** or part is intercepting the spiral* |
| `ch9:296` | 1 | *The map shows you where the **gates** stand.* |

**A reader meets `daemon` for nine chapters and then Appendix B calls them Gates.**
Appendix B is the workbook — the part she is meant to actually run — so the split lands
exactly where the vocabulary has to be reliable.

**This does not need the branch.** It is a rename across three files, and the branch's
version of it is a good model: `Gate:` → `Daemon:`, and `walk through them` → `befriend
them` at `ch9:296`, because you do not walk through a daemon.

## 2 · ch7 is the only one of the six Face chapters out of form

| | Section 1 | Section 2 |
|---|---|---|
| ch3 ch4 ch5 ch6 ch8 | **The Exile** | **The Distortion** |
| **ch7** | The Diplomat's Thesis | What It Is |

**Five of six, then ch7.** The branch's `1b83dab` conforms it and **writes ch7's missing
Distortion section** — roughly six paragraphs of new prose on what the village does with
honest terms once the Diplomat is gone, ending on the line the chapter is built to earn:

> *Presence becomes unconditional, which sounds like love and works like a tax.*

**This is the only genuinely new prose on the branch worth having**, and it is the one
item that cannot be reproduced by a rename. Everything else in ch7's 178 changed lines is
packaging around it: the EA table moves from Section 2 to Section 4, `Channel 1 —
Bridge-Builder` becomes `The Bridge-Builder`, and two moves get renamed (*Translate
Across Camps* → **Carry the Other Camp's Case**, *Repair After Rupture* → **Go Back
First**).

**It cannot be cherry-picked cleanly.** ch7 has changed under it for six days. The
Distortion section would have to be lifted as text and re-reviewed, which is the right
way to take it anyway.

## 3 · `ch9:200`'s heading is stranded

The branch retired `stage` as chapter-local vocabulary so it means the WAVE only.
**Master landed half of it.**

- `ch9:200` heading: **The Stage Sequence**: Review → Discern → Design → Build → Pass On
- `ch9:502` body: Review, Discern, Design, Build, Pass On **tracks how the work moves**

**The body already uses the branch's replacement phrase and the heading above it does
not.** The branch's rename is exactly `How the Work Moves`. One line.

`ch9:103` uses `stage` correctly — *the way the WAVE's stages do*. `ch9:410` is a third
sense (*that's the stage you're in*) and is a separate call.

---

## What is already in HEAD — do not re-take it

**Every chapter H1 the branch rewrote is already identical in HEAD.** Checked ch1, ch2,
ch3, ch5, ch6, ch9 — byte for byte. Master landed `26f4c9f`'s work independently.

**`about_the_author` is superseded.** The branch replaces the old bio with Wendell's copy
(*"meddling kid extraordinaire"*, Instagram handle). **HEAD carries a third version,
newer than both** — the Apple/Airbnb/T-Mobile paragraph, with *meddling kid* kept and the
handle dropped for `masteringallyship.com`.

**Appendix B's Direct Action gap is already closed.** The branch adds *Two More, for
Direct Action* plus a pre-campaign quest, **The Ask**, on the argument that the eight
quests reach only three of four domains. **HEAD has eleven quests and all four domains
are covered** — master closed the same gap differently. The three new quests are decent
and they are additions, not repairs.

**`From Card to Quest` and `Drawing Against the Shadow` are already promoted** to their
own sections in HEAD.

## What is unmerged and is a judgement call, not a defect

- **ch5 dissolving the Five Modes of Loyalty into the EA table.** Genuinely unmerged —
  `Modes of Loyalty` reads 1 on HEAD, 0 on the branch. This is a structural opinion about
  ch5 from six days ago and it deserves to be re-argued rather than merged.
- **ch7's Channels → Modes rename and the two move renames.** Ships with item 2 or not at
  all.
- **The three new Appendix B quests.** Additions.

## What is half-finished on the branch itself — do not take it

**`the school` → `the Horizon`.** The branch's `944bf3c` says *"R1 closed: the school is
the Horizon."* **Measured, the branch did 14 and left 49.** HEAD reads 11 Horizon against
**53** `school`. Taking a rename that is one-fifth done would leave the book worse than
either side, and the decision the commit claims to close is still open.

---

## Recommendation

> **Do not merge.** Seventeen conflicting files and all eight chapters, for three items,
> two of which are one-line renames.
>
> **Take item 1 now** — `Gate` → `Daemon` across `APPENDIX_B`, `APPENDIX_E` and `ch9:296`.
> It is a live vocabulary split in the shipping book, in the workbook the reader is meant
> to run, and it does not need the branch at all.
>
> **Take item 3 now** — one heading at `ch9:200`, where the body two hundred lines later
> already uses the replacement.
>
> **Take item 2 as text, not as a merge** — lift ch7's Distortion section, run it through
> the review, and decide the Face-form conformance on its own. ch7 being the only one of
> six out of form is a real gap and it is not going to close itself.
>
> **Then close the branch.** Everything else on it is either already in HEAD, superseded
> by something newer, or a six-day-old opinion that should be re-argued rather than
> merged.
