# DECISION LOG — the single editorial ledger

**Created 2026-07-31** per `specs/EDITORIAL_OPERATING_SYSTEM.md` and
`specs/SPEC_EDITORIAL_OS_INTEGRATION_2026-07-31.md`. Only items in this log
become editing work. Rejected ideas stay rejected. New rulings in spec
documents get entered here the day they are made; this file is the index, the
specs hold the argument.

Format: `ID | location | reader problem | evidence | decision | intended result | status`

## Standing rulings (seeded from the specs, decided)

| ID | Location | Reader problem | Evidence | Decision | Intended result | Status |
|---|---|---|---|---|---|---|
| DL-1 | book-wide | Voice defects flattening the prose | `MANUSCRIPT_FILE_CANON.md` standing rules | Banned: *room*, *quiet(ly)*, *genuinely*; no sentence-initial And/But; no negation stacks; A0 patterns banned | `gate.py` reads 0 on four surfaces | DECIDED, enforced |
| DL-2 | book-wide | Denying negations stage a beat instead of holding an axis | `MANUSCRIPT_FILE_CANON.md`, ruled 2026-07-29 | Four-move fix (cut / essence / synthesize / voice), run as one pass | 2 remaining book-wide | DECIDED |
| DL-3 | 536 construction sites | Constructions substituting for content | Three keep-tests, Wendell's ruling | Every site presumed cut; survives only on Function · Earned · Non-substitutable | Burden of proof on keeping | DECIDED |
| DL-4 | all treatises | Register drift read as defect | `FACE_AUTHORS.md`, ruled 2026-07-28 | The Face authors every section; bylines, not re-voicing | Drift converts to characterization | DECIDED |
| DL-5 | Head material | Heads read as exhibits, not people | `HEAD_VOICE_DIAL.md`, Wendell 2026-07-30 | Craft mastered, satisfaction still reaching, learning edge open and adjacent | Masters still learning, per the book's ethos | DECIDED |
| DL-6 | marginalia | Margin held to a lower bar than body | Ruled 2026-07-29 | The gate applies to the margin as its own scored surface | Both surfaces at 0 | DECIDED |
| DL-7 | book-wide register | Short-declarative register defended as voice | `SPEC_REPETITION_AND_CUTS.md`, three outside controls + *Igniting Joy* | It is drift, not voice; `prose_diet.py` targets *Igniting Joy*, never the book's own average | The bar is Wendell's voice working | DECIDED |
| DL-11 | book-wide | Copula density 2.14× the control | `SPEC_REGISTER_2026-07-29.md` | — | — | **CLOSED 2026-08-05 — re-measured with the regex that made the claim.** `prose_diet.py` reports against the book's own baseline and structurally cannot re-test a claim made against an external control, so `COPULA_JUNE` was run over all nine chapters instead. **copula/1k 61.4 → 46.2** (corrected measure 42.3, level with Elliott 40.9 and Chou 41.1). No longer 2.14× anything; 1.61× *Igniting Joy*'s 28.8. **Recorded and NOT opened as new work:** the two sibling metrics in the same spec table did not move — sentences ≤6 words **19.4% → 18.6%** against *Igniting Joy*'s 4.2%, and mean sentence length **16.0 → 15.8** against 18.8. The copula pass happened; the sentence-length pass never did. Nobody ruled those two, and the short declarative is arguably the book's voice rather than a defect. |

## Closed — verified by instrument 2026-07-31

**These three were seeded from `MANIFEST.md` and were already ruled.** The
manifest was stale; `build_book.py` and `grep` settle all three. This is the
repo's own standing lesson arriving on the first day the log existed: *do not
plan from a claim in a planning document without running the instrument.*

| ID | Location | Evidence | Ruling | Status |
|---|---|---|---|---|
| DL-8 | Appendix C | `appendices/APPENDIX_C_KEY_TERMS.md` header: retired 2026-07-30 by Wendell, off the spine, unreferenced. `build_book.py` seats Appendix C = `APPENDIX_C_FIVE_CHANNELS.md`, 1,206 words | Glossary retired; C is The Five Channels in Practice | CLOSED |
| DL-9 | `ch3:415` (was cited as `ch3:400`) | Reference reads *Appendix C: The Five Channels in Practice*; that letter now exists; `build_book.py` reports no UNPLACED | Lettered. Reference resolves | CLOSED |
| DL-10 | "Head Sura" | Appears in **zero** chapter files and **zero** marginalia insertions. Only `FACE_AUTHORS.md` quoting a note that never shipped | Not a canon conflict and not a print blocker. Spec hygiene only | CLOSED |

## Open items that block or shape print

| ID | Location | Reader problem | Evidence | Decision | Intended result | Status |
|---|---|---|---|---|---|---|
| **P0** | `ch1:181`, `ch1:269`, `ch7:500` | **Three placeholders print verbatim, including an authoring note addressed to Wendell** | `placeholders.py` on merged master 2026-08-01: **3 hits, all still present.** `gate.py` passes clean on all four surfaces — it has no placeholder rule | ch7 testimony slot **CUT** (ruled; DL-19 Move 4 relocates the passage to `ch9:342`); ch1 visual marker **CUT**; ch1 app CTA **DEFERRED by Wendell**, links still being set up | Nothing addressed to the author reaches a reader | **BLOCKS PRINT — 3 sites.** Two are ruled with approved drafts and **unapplied**; the log previously read *2 of 3 closed*, which was a ruling mistaken for an edit. Application is held by DL-18 |
| **P0-b** | six shipping appendices — A, B, C, D, E, F | **Internal production metadata prints in the book** — file paths, approval records, a *Draft* status flag, and an instruction to the production team | 15 lines verified in `build/MTGOA_PRINT_2026-07-31.md`; `gate.py` and `build_book.py` both pass. `placeholders.py` extended and now reports 21 | **FIXED 2026-07-31.** `build_book.py` strips provenance in `read()`, the single funnel every component passes through. `placeholders.py` applies the same strip and keeps the rule as a regression guard — silent normally, fires if the strip breaks | Provenance stays in the repo, never reaches a reader | **CLOSED — 0 metadata lines in the build** |
| DL-16 | `back_matter/kickstarter_backers.md` | Backers promised a credit do not find one | `build_book.py` — GAP, spine assembles with 2 gaps | Supply the list, or drop the component | Spine builds with no gaps | **CLOSED 2026-08-05 — see DL-48.** Wendell: *"we're not doing kickstarter backer page."* GAP → OPTIONAL, file never written |
| DL-17 | `back_matter/enrollment.md` | Reader finishes the book with no next step | `build_book.py` — GAP; `MANIFEST.md` notes it waits on R1 | Write it, or ship without it | Spine builds with no gaps | **CLOSED 2026-08-05 — see DL-48.** Written as `What Comes Next`, 343 words, on the spine |
| DL-18 | branch protocol | Two sessions editing the same nine files produce work that cannot be cheaply reconciled | `editorial_reports/2026-07-31/BRANCH_COLLISION_CHECK.md`; swmp78 shares our merge-base and edited ch1–ch8 today; both branches merge clean at **0 conflicts** because file sets are disjoint | **This branch stays apparatus-only.** Merge it first (pure addition), then apply A2/A4 where the chapters are owned | No repeat of the 91-hunk collision | RULED |
| DL-12 | four `⟦…⟧` register tokens | None in canon | `gate.py` tokens column reads 0 on all four surfaces | Due only if the register pass extends to the other five Heads | — | DEFERRED, not gating |

## Book Architect scan — 2026-07-31

Full report with quoted evidence and disproof tests:
`editorial_reports/2026-07-31/ARCHITECT_WHOLE_BOOK.md`. Diagnosis only; no prose
was changed and the gate stayed green. Each of these is a Chapter 1 promise
measured against the eight chapters after it, or a prerequisite arriving after
the reader needs it.

| ID | Location | Reader problem | Evidence | Decision | Intended result | Status |
|---|---|---|---|---|---|---|
| A1 | `ch1:149`, `ch1:173`; 0 hits ch2–ch9 | Jordan sorts her quest into Chance/Skill/Passion and the taxonomy never returns | *"Everything ahead is built to strengthen your hand in all three"* — measured 0 occurrences in eight chapters | Narrow the ch1 claim, or seat the taxonomy | The book's stated spine is true | **CLOSED 2026-08-05 — no defect; see DL-65.** The finding measured vocabulary recurrence and read it as a broken promise. ch1:207 hands the taxonomy off inside ch1: *"You just sorted your situation into three kinds of game. Now meet the six who can teach you to play it."* Three games are the board, six Faces are how you play, and the Faces carry the remaining eight chapters. |
| A2 | `ch1:207`, `ch2:560`, `ch9:682`; silent ch3–ch8 | The sheet ch9 turns back to is nearly empty | *"a line added in every chapter ahead"* — promised 8, delivered 2 | Narrow the promise, or seat one line per chapter using ch2:560 as the template | The reader's record of her own change actually accumulates | **CLOSED 2026-08-05 — self-closed and verified.** The chain was seated at some point after this row was written and nobody came back to mark it. ch2 through ch8 each carry *"Add a line to the sheet"* and each one chains to the last: *under the daemon → under the channel you skip → under the line you have not drawn → under the inheritance → under the harm you keep fixing → under your walk-away price.* Seven lines, cumulative, and ch9 collects them: *"Chapter 1 told you this would happen and left a line open on your character sheet for it. Fill it in now."* Promised 8, delivered 2 at the time of writing; delivered 7 plus a collection now. |
| A3 | `ch1:68`, `ch1:73` | *Helping the less powerful* — the paternalism myth — is named and never taken apart | 9 of 10 myths map to a teardown; this one returns 0 hits ch2–ch9 | Soften *"each one"*, or assign the orphan | The stated inventory is accurate | **CLOSED 2026-08-05 — no defect; see DL-65.** The teardown exists at `ch2:292` and again in the Second Walk ported by DL-56. The row's own evidence was a phrase count, and a phrase count cannot see a teardown that never repeats the phrase. |
| A4 | `ch8:183`, `213`, **`223`**, `297`, `307`, `327` | Five-term developmental scale, undefined, at **six sites**; `ch8:223` uses it as the answer-set to the Sage's central diagnostic | *"all the altitudes — Red, Amber, Orange, Green, Teal"*; `termdebt.py` NEVER DEFINED; Magenta absent book-wide | **Wendell 2026-07-31: CUT.** Replacement must carry the same weight — the six Faces are the taught answer-set, per `ch8:213`/`ch8:185` | No jargon without translation, and *which altitude is this?* still answerable | **RULED 2026-07-31 — AND NOT APPLIED.** Re-measured by the Pass 2 Continuity and Claims audit, 2026-08-01: the five-term scale is still on the page at `ch8:224` and `ch8:262`, *Teal* alone at four more sites, and `termdebt.py` reports it first used 114 lines before it is glossed while *Amber*, *Orange* and *Green* are never defined at all. Filed as **CA-1**. A ruled cut the ledger reports as done is worse than one never ruled. **APPLIED 2026-08-01**, ten months of vocabulary out in seven edits: 0 occurrences of Red, Amber, Orange, Green or Teal anywhere in `manuscript/`. |
| ~~A5~~ | `ch2:558`, `ch3:786`–`800` | **WITHDRAWN 2026-07-31 — the finding was wrong.** Domains are named with an Appendix A pointer in ch2 and defined in ch3 with a *You're winning when* test each. `termdebt.py`'s bold-gloss rule cannot match a gloss inside the bold span, so other NEVER DEFINED rows are suspect | — | — | — | CLOSED — no work |
| ~~A5-orig~~ | `ch3:772`, `791`, `795`, `799` | "One of the four" back-references a set she has not met | Domains NEVER DEFINED in body; defined only in Appendix A; ch3's own italic routing convention at `415`/`560`/`589` is not applied here | Add the pointer in the existing convention | Prerequisite reachable where needed | **CLOSED 2026-08-05 — superseded by A5, which withdrew this finding as wrong.** Kept only as the original wording. The domains carry a *You're winning when* test four times in ch3. This row has read OPEN since 2026-07-31 next to the row retracting it. |
| **A6** | `ch1:193` only | **RULED + DRAFTED 2026-07-31. Scope collapsed from three edits to one.** *The loop* is not this book's name for anything — every use means a different cycle, and `ch9:103` rules that the WAVE does *not* loop. ch2's labelling is honest; ch3's recall is earned 582 lines after the teaching. The referent was never missing; the **name** was never adopted | *loop* means a rumination pathology (`ch2:172`), a four-stage cycle (`ch6:269`), and the five modes (`ch8:438`, `ch9:103`). The WAVE runs 10–18 times in every Face chapter | **Name it the WAVE in ch1 and point at ch3.** One edit, two sentences | The book's central promise is named correctly at first mention | **DRAFTED — in the approved set** |
| ~~A7~~ | `ch3:228`, `appendices/ON_THE_SHOULDERS_OF.md` | **CLOSED 2026-07-31 — already fixed on `claude/mtgoa-manuscript-changes-swmp78`**, found independently by their citation audit. Kishimi and Koga credited in Appendix G with the disagreement named. Do not duplicate. Their audit found four further Appendix G credits pointing at absent sources — Wendell's call | — | — | CLOSED elsewhere |
| ~~A7-orig~~ | `ch3:228`, `front_matter/copyright.md` | A named outside source carries no credit anywhere in the shipping book | *The Courage to Be Disliked* is quoted at `ch3:228`; returns **zero hits** in the copyright page's Sources and permissions, Appendix G, and all back matter | Credit it, or cut the attribution | Every source the book owes is credited | **CLOSED 2026-08-05 — superseded by A7.** Kishimi is in Appendix G and `citation_audit.py` carries the exemption with its reason. Same defect as A5-orig: a superseded row left reading OPEN beside the row that closed it. |

## This session (2026-07-31)

| ID | Location | Reader problem | Evidence | Decision | Intended result | Status |
|---|---|---|---|---|---|---|
| DL-13 | `SPEC_EDITORIAL_OS_INTEGRATION` §4 | Editorial passes lack a one-page brief to judge against | Lean OS anchor 1; pieces existed in `THESIS_DRAFT.md` + Jordan ICA | Confirm or amend the assembled book brief | Every role prompt gets the same brief | **CLOSED 2026-08-05 — see DL-71.** Confirmed with three amendments and moved to `specs/BOOK_BRIEF.md`, which is the one page the row asked for. |
| ~~DL-14~~ | `specs/VOICE_ANCHOR.md` | — | Lean OS anchor 2 | **CLOSED 2026-07-31.** Wendell selected five: `ch1:10` · `ch1:119` · `ch4:115` · `ch5:516` · `ch8:342`. `ch4:115` is in deliberately to keep humor in the comparison set | Voice Guardian can run; first pass done — 22 ACCEPT, 2 REVISE, 1 accepted with a recorded loss | **CLOSED** |
| **DL-19** | `ch3:258`, `ch5:308`, `ch5:516`, `ch6:365`, `ch7:508` | **Two first-person authors share the body text with nothing to tell them apart.** In ch7 the collision is total: Elian Cross recounts cases with a cost at `ch7:130`–`136` (*"Forty-one cases and I have not said it once"*), which is the exact shape a Wendell testimony would take | `specs/SPEC_DL19_AUTHOR_COLLISION_2026-07-31.md`. Full scan: four sites in ch3–ch8, none in ch4/ch7/ch8. Two comply already (Face writing under the chapter's assigned daemon, per `FACE_AUTHORS.md` field 3); two carry real-world referents a Face cannot own. Marginalia is not the destination — the annotator is an in-world character, revealed as Headmaster at `MARGIN_ARC.md:141` | **Wendell 2026-07-31: enforce DL-4.** Four moves: ch3:258 to second person; ch5:308 re-voiced in two phrases; ch5:516 and ch6:365 unchanged with the reasoning recorded; ch7:508 slot cut and the shame passage relocated to `ch9:354` | One author per surface. Wendell's biography lives in the frame — ch1, ch2, ch9 | **RULED — drafts filed, application blocked on DL-18** |
| DL-15 | this file | Rulings scattered across seven-plus specs get re-argued | Lean OS anchor 3; July precedent | **CLOSED 2026-08-05 — self-closed by use.** 79 rows, every ruling since 2026-07-29 entered here, and DL-47 through DL-60 were all argued against this file rather than against a spec. Adopt this log as the single ledger; spec rulings sections feed it | Rediscovery loops end | OPEN, awaiting Wendell |

## 2026-08-01 — ship day

| ID | Location | Reader problem | Evidence | Decision | Intended result | Status |
|---|---|---|---|---|---|---|
| **DL-20** | all shipping surfaces | **The book routes readers to a product that is not shipping with v1.** A wrong pointer in a printed book cannot be patched, which is what separates this from every other open finding. | `instruments/shipcheck.py` returns **51 sites**: 13 `→ app` tags in Appendix B, the B2 formula ×6, three practice-prompt tails, ch3's App Layer section, ch1's six-site cluster including the thirty-day trial, ch9's eleven bars-engine references, `about_the_author`. | **Wendell 2026-08-01: the app removal is blocker one.** It outranks placeholders, build gaps and every quality finding. `SPEC_REMOVE_APP_V1` §5's three open rulings are now the critical path. | The shipped book points at nothing it cannot deliver. | ****CLOSED 2026-08-05.** Ruled and drafted portion applied 2026-08-01 (57 → 20 in `manuscript/`); the remainder closed by DL-48, which cut the bars-engine line from the author bio — the last app-routing site in the book. `shipcheck.py` category 1 has read **clear** since. |
| **DL-21** | `instruments/shipcheck.py` | The editorial system ranks findings by what they cost to fix, not by whether they stop the book. On ship day those are different questions and only one of them was instrumented. | `rescan.py --list` orders by claim-error → blocked → continuity → verify → structural → line. Nothing in the repo reported ship state as a single board. | Adopt `shipcheck.py` as the ship-goal view, running alongside `rescan.py` rather than replacing it: six categories, ordered by DL-20, each answering "does this reach the reader wrong." | One command answers whether the book can ship, and quality work stops competing with blockers for attention. | **ADOPTED 2026-08-01.** |
| **DL-19** | `ch3:258`, `ch5:308`, `ch5:516`, `ch6:365`, `ch7:508` | (see above) | — | — | — | **APPLIED 2026-08-01** on `swmp78`. M1, M2 and M4 seated; M3 confirmed no-change. M2 needed a third site the spec did not scope. |
| **DL-22** | 33 sites in ch1–ch9, listed in full in `editorial_reports/2026-08-01/PASS3_LINE_EDITOR.md` §4 | **Readability defects that survive whatever the open structural rulings decide** — a pronoun with no antecedent, a modifier attached to the wrong clause, an instruction written as a description, one sentence printed twice verbatim. | Pass 3 · Line Editor scan against the merged manuscript at `ee18459`. `instruments/line_scan.py` (new) returns 186 candidates on six rules; reader adjudication plus every carried-over LINE and BLOCKED finding from 2026-07-31 reduces that to 33 flags. 16 July line findings verified **closed by edit**; CH7 L2 withdrawn as a false flag. | **Wendell 2026-08-01: "All of these are blockers to be updated." All 33 are work.** LE-3's proposed edit rejected in the same ruling and rewritten — the first version nested the quoted line inside the sentence and could not be read aloud; the replacement quotes it in subject position. Applied by `instruments/pass3_apply.py`, exact anchors, aborts on a missed or duplicated anchor. | A reader stops doubling back for what the sentence should have handed her, and the voice is not smoothed to get it. | **APPLIED 2026-08-01.** Gate 0 on four surfaces before and after · `dupes.py` 1 → **0** · `line_scan.py` 186 → 182 · review.py BLOCK 13 unchanged · ch5 passive back to 1.35 after LE-17 was reworked off a passive. **Branch is no longer apparatus-only** — it now edits nine chapter files and must be sequenced, not merged in parallel, per DL-18. |
| **DL-23** | `instruments/rescan.py` | **The working list was reporting fixed lines as live work.** Its LIVE test was `norm(quote)[:60] in corpus` — the first sixty characters of a quotation, roughly a clause — and a fixed defect almost always sits later in the sentence than that. | Measured 2026-08-01: `ch1 L2` ("a point of it" → "a penny of it"), `ch1 L4` (the "win and succeed" doublet, gone), `ch1 L6` ("quieter", gone), `ch6 L1` ("the The Emotional Body", gone) all reported **LIVE**. 42 of 250 quotations matched on prefix only. | **CORRECTED 2026-08-01.** Split each quotation on sentence ends and on the ellipsis the reports use for elision, then test each piece; a two-sentence quote with one sentence rewritten now reads PARTIAL rather than LIVE. Rationale written into the file's docstring next to the rule. | 102/31/39 LIVE/PARTIAL/GONE becomes 85/48/39; 41 verdicts change and no finding enters work that was not already there. | **APPLIED 2026-08-01.** |
| **DL-24** | `instruments/line_scan.py` `orphan-ref` | The rule flagged the fix it caused. LE-3 repaired a cross-block pointer by quoting the line it pointed at (*This isn't working* is a verdict on the work), and `orphan-ref` matched the demonstrative and called the repair a defect. | Book-wide count moved 6 → 7 on an edit that removed an orphan reference. | A demonstrative inside an italic quotation carries its antecedent with it; the rule no longer accepts an italic opener. Bold still counts, because bold marks emphasis on the book's own sentence rather than a quotation. | A finder that does not manufacture work by being fixed. | **APPLIED 2026-08-01.** Count back to 6, both real. |
| **DL-25** | the marginalia surface — 66 blocks, 6,656 words, `manuscript/ch2.md`–`ch9.md` | **The frame had never had a line pass.** DL-6 rules it a separately scored surface; `line_scan.py` stripped it, `prose_diet.py` strips it, and `marginalia/review.py` reads it for voice rather than readability, so no instrument had ever asked the Line Editor's question of it. | `instruments/line_scan.py --margin` (new mode, reports the block type with every hit because MARGINALIA / HANDBOOK / EPIGRAPH-BYLINE / POSTCARD / SIGNATURE are five genres). 9 mechanical candidates, 1 real; the other five flags came from reading all 66 blocks. Measured: **one exclamation mark in the whole surface** (`ch4:17`), and it lands on the formula the other five handbooks close with a period; **0 repeat, 0 doubled, 0 banned-kin** — the frame is cleaner than the body on every mechanical count. | **Wendell 2026-08-01: the margin gets its own pass, not a column in the body's.** Six flags filed in `editorial_reports/2026-08-01/PASS3_MARGIN_LINE_EDITOR.md`; MG-6 is recorded with a recommendation to leave. MG-5 — *eleven* used by three separate speakers as a tenure — is a ruling on Wendell's own running gag, not an edit. | The annotator's hand is held to its own bar, and `orphan-ref` stops being read as a defect on the one surface where it is the genre. | **APPLIED 2026-08-01 — Wendell: "apply them both places."** MG-1 to MG-5 written to `insertions.py` **and** `manuscript/` by `instruments/margin_apply.py`; MG-6 left as recorded. `compile.py --check` green, 66 blocks before and after, gate 0 on the marginalia surface, exclamation marks 1 → 0, body surface untouched. **Residual flagged, not touched:** `eleven years` still runs at `ch7:136` (body, Elian), `ch7:741` (annotator) and `ch8:200` (Elian) — the ruling covered Bram's line, and the remaining choice is the same kind of call. |
| **DL-26** | `marginalia/insertions.py` ↔ `manuscript/` | **The margin exists twice and an edit to one copy is silently lost.** A margin edit made only in `manuscript/` survives until the next `compile.py --strip`/`--apply` cycle and is then reverted; one made only in `insertions.py` never reaches the book. | Verified 2026-08-01: the flagged strings (`have suffered as a result!`, `did not used to`) are present in both files. `compile.py --check` green, all anchors resolve across eight chapters. | Every approved margin edit is applied to **both** files and `compile.py --check` re-run. A `--strip`/`--apply` cycle is rejected as the route for a small edit: it rewrites all 66 blocks and makes the diff unreviewable. | A margin fix stays fixed. | **CONFIRMED AND USED 2026-08-01.** `margin_apply.py` enforces it: an anchor missing from either copy aborts the run with nothing written. |
| **DL-27** | the 33 Pass 3 edits | **A line-edit batch had gone in without its gate.** The Lean OS makes the Voice Guardian the final check on any passage a line pass changed, and it had never run on these; the risk it exists for is prose that is technically cleaner and less alive. | `editorial_reports/2026-08-01/PASS4_VOICE_GUARDIAN.md`. Judged against `specs/VOICE_ANCHOR.md` (DL-14's five passages) and the §4 book brief. **Wendell 2026-08-01: exempt the treatise sections** — already canon in `VOICE_ANCHOR.md` §Scope, now drawn mechanically at the `<!-- HANDBOOK -->` / `<!-- SIGNATURE -->` boundaries. 10 exempt, 23 judged. `review.py --mode voice` BLOCK 13, unchanged by the batch. | **18 ACCEPT · 5 REVISE MINIMALLY · 0 RESTORE ORIGINAL.** LE-4 fails on meaning — inserting *often* produced *the ones who do not often push through*, the opposite of the sentence, so the flag was closed without being fixed. LE-9, LE-14 and LE-26 each fixed their defect and cost the paragraph its last beat, which the anchor's own rule names as failure. LE-7 broke the six-chapter `*Card Name* becomes:` formula at its first appearance. | An edit that improves every counter and flattens the voice does not reach the book. | **ALL FIVE APPLIED 2026-08-01.** LE-4 went in with the pass as a live regression; LE-7, LE-9, LE-14 and LE-26 followed on Wendell's word. LE-7 resolved without a judgment call: ch3's Move 5 is *Say the Thing Under the Thing* (`ch3:784`), which is what the worked example does, and every other chapter's quest example already uses its own Show Up move. `Aim the Awareness` — a card named once and taught nowhere — returns **0 hits**. Gate 0 on four surfaces. |
| **DL-28** | whole book — nine chapters, seven appendices, front and back matter, 120,141 words | **The Continuity and Claims Auditor had never run book-wide.** The July reports carry a continuity section per chapter and `citation_audit.py` mechanised the claims half, but the role itself — one auditor, the whole book, the canonical terms in hand — had not, and the OS puts it *before* line polishing precisely because central claims gate line work. Pass 3 ran twice ahead of it. | `editorial_reports/2026-08-01/PASS2_CONTINUITY_AND_CLAIMS.md`. Instruments first: `termdebt.py`, `citation_audit.py`, `build_book.py`, `chain2.py`, `dupes.py`, `repeat.py`, `rescan.py`. **13 of the July claim-errors verified closed by edits made since**, by string search rather than by status column. `chain2.py` reports 0 bare claims across 25 moves. | **5 ERROR, 3 VERIFY, 11 instrument findings withdrawn.** CA-1 is A4's ruled cut, unapplied. CA-2: *five channels* names the wu xing channels and the Diplomat's set. CA-3: ch9's H1 is not its canon name. CA-4: three H1 formats across nine chapters. CA-5: Gendlin's *felt sense* used at seven shipping sites, credited only in the further-reading appendix, while the copyright page names Elliott and Meadows for the same reason. | The book stops contradicting itself where a printed page cannot be patched. | **APPLIED 2026-08-01 — Wendell: "let's make the changes for these."** 30 edits across five files by `instruments/pass2_apply.py`. Altitude colours in `manuscript/`: 7 sites → **0**, and `termdebt.py` now reads 0 uses on all four colour rows. `citation_audit.py` 9 borrowed → 8. Gate 0 on four surfaces, `compile.py --check` green, build gaps unchanged at 2. Four sites the first run missed — `Green` at `ch8:270` and three Diplomat-sense *channels* in ch7 — were caught by re-running the instruments **after** applying and are folded into the script. ch2's trailing H1 clause flagged and left. |
| **DL-29** | `instruments/termdebt.py` and `instruments/citation_audit.py` | **Both rules over-fire in a repeatable, nameable way, and one of them has now cost two findings.** `termdebt` matches copula, roster, table, heading and inline glosses but **not appositives**, so it reported *Vulnerable Child* as never defined when `ch2:434` defines it at first use — the same miss that withdrew A5 on 2026-07-31. `citation_audit`'s DEAD rule measures Appendix G, whose stated job is to list books the text does not quote. | Measured 2026-08-01: 1 of `termdebt`'s NEVER DEFINED rows and 10 of `citation_audit`'s 11 findings do not survive reading. The eleventh, Gendlin, is real, which is why the rule keeps its place. | Teach `termdebt` the appositive shape before a third finding is withdrawn on it; exempt Appendix G from DEAD, or rename the category to what it actually measures. | An instrument that manufactures findings costs more than one that misses them. | **CLOSED 2026-08-05 — see DL-70.** Was: OPEN, not started, recorded against the next instruments session. |
| **DL-30** | the nine chapter H1s and subtitle lines | The Pass 2 audit left one variance open: ch2's H1 carries a trailing clause — *THE FOREST — Why Allyship Keeps Failing (and Where to Start)* — where the other eight stop at the Face name. I flagged it as a format inconsistency rather than cutting it, because the clause says something the subtitle line does not. | Measured 2026-08-01: **all nine chapters carry an italic subtitle line at line 2** and always have. ch2 is the only one that also carries a clause in the H1 itself. | **Wendell 2026-08-01: "honestly I do like all the chapters having these kinds of subtitles."** The descriptive subtitle is wanted, not tolerated. ch2's H1 clause **stays**; nothing is stripped and nothing is added. CA-4 closes on the format fixes already applied — caps-colon across all nine — and the trailing clause is ruled a feature. | The reader gets told what a chapter is about before she starts it, on every chapter. | **RULED — no change made.** If the intent was to give all nine an H1 clause of ch2's kind, that is eight new lines of authorial copy and wants its own drafting pass. |
| **DL-31** | `manuscript/ch3.md:2` and `manuscript/ch4.md:2` | **Two consecutive chapters each claimed to be the foundation.** ch3 read *Emotional Alchemy as the Foundation of Real Allyship* and ch4 read *The Clean "No" as the Foundation of Real Allyship*. No pass had flagged it; it surfaced while drafting H1 clauses. | **The book had already ruled it, in the six admissions handbooks.** The School of the Body (ch3) is the only school whose entry clause reads *"None. Everyone begins here, including those who arrive certain they are past it."* Every other school requires prior standing at the one before it — the Line at the Body (*"If you cannot, the School of the Body takes first-years every season"*), the Oath at the Line, the Pattern at the Oath, the Bridge at the Pattern, the Horizon at all five. ch4's subtitle contradicted ch4's own admissions page forty lines below it. | **ch3 keeps the foundation claim. ch4 becomes *The Willingness to Be Unwelcome*** — the chapter's own named gift, at `ch4:119`, `ch4:587`, `ch4:755` and `ch4:763`, and its EA table's dissatisfaction pole at `ch4:272` (*fear of being unwelcome → Wonder*). **Wendell 2026-08-01: "willingness to be unwelcome hits. It connects to the courage to be disliked in a way that's quite powerful."** That debt is already owned on the page: Appendix G credits Kishimi and Koga's *The Courage to Be Disliked* with the departure named, and ch3:264 quotes the line it departs from. | One foundation, and a subtitle that says what its chapter actually teaches. | **APPLIED 2026-08-01.** One live copy — `BYLINES.md`, the source notes, `chapters/` and `compiled/` are process history; `insertions.py` does not carry it. Gate 0 on four surfaces, `compile.py --check` green. Rejected alternatives recorded: *That Keeps the Game Playable* collides with ch8's named move at `ch8:715`; *What's Worth Keeping* walks into the Regent's word. |
| **DL-32** | the eight chapter H1s | Wendell wanted every chapter to carry a plain-English clause of the kind ch2 has had all along (DL-30). Eight chapters did not have one. | Drafted against each chapter's own stated argument — `ch1:123`, `ch3:691`, `ch4:753`, `ch5:658`, `ch6:577`, `ch7:187`, `ch8:69`, `ch9:618` — reviewed in console before anything landed, and each checked against its own subtitle so the two lines do not say the same thing twice. ch1 went three rounds: the debt framing was rejected as not landing, and the first replacement hit *play* three times across the H1 and the subtitle, which is Wendell's ear and correct. | **APPLIED 2026-08-01.** ch1 *What You Spend, and What Comes Back*, mirroring the chapter's own two section headings · ch3 *What to Do With What You Feel* · ch4 *Who Pays When Nobody Says No* · ch5 *What You Inherited, and What You Do With It* · ch6 *Fixing the Condition Instead of the Person* · ch7 *Why Being Good at Care Is Only Half of It* · ch8 *Seeing the Whole Board Without Leaving the Table* · ch9 *Building Something and Putting It in Front of One Person*. ch2 unchanged. | Every chapter tells the reader what it is about before she starts it, in the TOC and in the running head. | **APPLIED.** Gate 0 on four surfaces, `compile.py --check` green, spine assembles. Deliberately **not** used: *the view from above was the easy half*, ch8's landing line and a `VOICE_ANCHOR.md` passage — a landing line spent in a title is a landing line lost. `MANIFEST.md:52` corrected in the same commit; it still carried ch9's retired name. |
| **DL-33** | `instruments/build_book.py`, the generated contents page | The eight new H1 clauses reached the TOC by concatenation, so every chapter line carried **two em-dashes** — one the contents page's own punctuation separating label from title, one belonging to the title — and the Face names shouted in caps directly above an appendix block in title case. | Measured on `build_book.py --toc` after DL-32 landed: `**Chapter 1** — THE INFINITE ARCADE — What You Spend, and What Comes Back`. | `toc_title()` added: split the H1 on its em-dash, hold the clause with a colon, and bring an all-caps Face name down to title case. The manuscript files are untouched — SHOUTING is a manuscript-file convention and a contents page is not one. The TOC is generated, so there was never a static file to update. | One list, one dash per line, chapters and appendices reading as the same page. | **APPLIED 2026-08-01.** `**Chapter 1** — The Infinite Arcade: What You Spend, and What Comes Back`. Spine assembles, 2 known gaps. |

## 2026-08-02 — the inchoative counter

| ID | Location | Reader problem | Evidence | Decision | Intended result | Status |
|---|---|---|---|---|---|---|
| **DL-34** | `instruments/prose_diet.py`; 31 sites in `manuscript/ch1.md`–`ch9.md` | **The book's most-tightened counter had a blind spot the size of a construction.** `go / goes / going / went / gone` + adjective is an inchoative copula — it means *become*, and `goes cold` unpacks to `is cold now` — but it carries a verb of motion, so the eye reads it as something happening. With an inanimate subject nothing happens and nobody caused it, which makes *the meeting that goes sideways* the same defect as *the hum arrives*, wearing a costume that reads as active. | **Wendell 2026-08-02**, on a sentence of mine that had just passed `gate` clean at `copula 0.46`: *"What does 'go careful' mean? In which way are we now using the go, went construction as a way to be passive in a way that seems active?"* Measured on two files of otherwise identical sentences: *"The meeting goes cold. The table goes careful."* scores **be 0.00, copula 0.00**; *"The meeting is cold. The table is careful."* scores **be 4.97, copula 3.44**. `BE` and `COPULA` match only `is\|are\|was\|were\|be\|been\|being`, so the construction is structurally invisible to both. **Driving `copula` down actively rewards moving copulas into `go`.** | **`inchoative` added**, scored on inanimate subjects only, because the construction is legal when a person becomes something (*you go cold*, `ch2:358`, the Emotional Body freezing — the involuntariness is the point) and a defect when the subject cannot act. A candidate finder like `AGENT`: the counter scores, `-v` prints every site with the subject it found and the line number, and a reader rules the edges. Baseline **measured**, not typed: 0.30 per thousand, 31 defect sites in 101,821 words. | New prose stops hiding copulas in a verb of motion, and the six existing sites get read. | **APPLIED 2026-08-02.** Book-wide: ch2 **1.07 (3.56x)**, ch1 0.40 (1.34x), ch3 0.38, ch4 0.35, ch8 0.29, ch6 0.28, ch5 0.10, ch7 0.08, ch9 0.08. **Ch2 runs 3.5x the book and 13x ch9** — the chapter this was found in is the chapter that has it worst. |
| **DL-35** | `instruments/review.py` `book()` | **The book-wide diet step could not fail, and printed one finding as though it were the summary.** `prose_diet.py` exits 0 whatever it finds — heaviness is a candidate, not a build failure — so the generic loop marked the step `ok`, and its `tail` variable printed only the **last line** of the heavy block. | Found 2026-08-02 while wiring DL-34 in. On the day it was found the book carried three heavy counters and `review.py` reported one of them, over the word `ok`. **The same failure the file's own comment forty lines above describes**, in the other half of the same file: `draft()` was taught to parse the `heavy:` block when the "heavy (>1.30)" rename broke it, and `book()` was never taught. | Special-case the diet step in `book()`: parse the `heavy:` block as `draft()` does, print every line, count each as a finding. Rationale written into the file beside the rule. | A step that cannot fail stops reading the same as a step that passed. | **APPLIED 2026-08-02.** `2 diet LOOK 3 heavy` — ch2 inchoative 3.56, ch7 passive 1.44, ch1 inchoative 1.34. `ch7 passive 1.44` had been on the page and unreported since the counter landed on 2026-07-31. |
| **DL-36** | `.claude/skills/mtgoa-review/SKILL.md` step 3 | **The slop pass was being run on half of itself, and the half being skipped is the half that catches invented prose.** `/no-ai-slop` carries a pattern list in `SKILL.md` and a checklist in `eval.md`; auditing against the patterns finds bad sentences and cannot find fabricated ones. | **Wendell 2026-08-02**, on a ch2 draft reported clean by that half-pass: *"Did we run a /no-ai-slop on the 'new' it's failed in a lot of ways… Who is the somebody. What is the hard subject. Which agenda. 6 of you? Why six?"* The draft read *"Six of you built the group to hold exactly this"* — a headcount invented out of nothing and asserted as the reader's history, which `MANUSCRIPT_FILE_CANON:154` bans by name and which `gate.py`'s A0 counter cannot see (it matches four fixed phrases). `eval.md` check 1 — *without adding claims, examples, stats* — is the check, and it is step 4 of that skill's own workflow. | Step 3 now requires `eval.md`, and records the two failures the whole pass is blind to: **fabricated specificity** (the legal form is `ch1:22` — an open menu, generic actors, specificity in *what happens*; you cannot make a hypothetical concrete by adding details) and **banned-word laundering** (the sentence wanted *goes quiet*, `quiet` is banned, `careful` went in, the gate passed clean; `inchoative` tags six of these LAUNDER and that is the nearest a machine gets). | A draft cannot reach Wendell carrying a scene nobody lived. | **APPLIED 2026-08-02.** The vaguer draft it replaced was the more honest one, which is the lesson worth keeping: **vagueness admits what it is; a definite article over a blank does not.** |
| **DL-37** | `instruments/agency_registry.yaml` `exceptions: E-1`; `specs/SPEC_JORDAN_METAPHOR_ARC_CH1_CH3_2026-08-02.md` §5 | **Two documents written the same day gave opposite instructions about the same sentences.** E-1, the education-by-emotions register, licenses `teach`/`say`/`report` on a channel as an ontological exception — *"fear teaches you what you actually care about"*, *"Sadness says: this was true."* The Jordan metaphor spec's `[METAPHOR_OVERREACH]` names *"deliver a message"* phrasing as a risk of treating feelings as infallible authorities and requires a feeling be framed as something to investigate rather than obey. E-1's own boundary clause says the channel delivers a **true** content, which is the exact word the Jordan item objects to. | Both dated 2026-08-02. The Jordan spec was drafted from `master` at `b2ceaf8`, which did not carry E-1 — the exception lived on the agency branch, so the conflict was invisible when the item was written. Surfaced 2026-08-03 while reading the two specs master merged in via PRs #2 and #3. The basis for E-1 is ch3:207 stating the thesis outright: *"Other practices manage emotions. Other practices optimize them. This one is the education by emotions."* Nine sites, three verbs, across ch3, ch4 and ch9. | **Wendell 2026-08-03: "sadness says stands as ontology, E-1 wins."** The liturgical register keeps its authority-voice. `teach`, `say` and `report` on a channel ship unhedged, with no corrigibility qualifier. The Jordan spec's `[METAPHOR_OVERREACH]` item is **withdrawn, marked do-not-implement in place** rather than deleted, so the reasoning survives for anyone who meets the same tension later. | A future session working the Jordan spec cannot hedge the book's signature device without knowing it is overturning a ruling. | **RULED 2026-08-03 — recorded in three places.** `precedence` block added to E-1; the spec item struck through under a RULED banner; this row. **The `[ETHICAL_GAP]` in the same section survives untouched and is annotated as still live** — E-1 licenses a channel delivering a content about *its own bearer*, while that gap is about the reader naming a channel in *someone else*. Different act, different risk, and E-1 does not shield it. |
| **DL-38** | 16 headings across `ch2`–`ch9`; `instruments/agency_registry.yaml` `registers` | **The R8 village pass converted bodies and left every heading above them singular**, and two censuses had flagged the leftovers as keep-candidates without ruling them. Left unruled, a later session meets *What the Village Does With Tradition* over a body that says *the villagers* and reads it as an unfinished pass. | Scanned every heading in `manuscript/` and `appendices/`: **16 contain "village", and no other abstraction holds a heading anywhere in the book** — so the village set is the whole question, not a sample. They are three different things. **Arena (4)** — `ch2:166` *Forest vs Village*, plus the three *in the Village* practice headings — is the book's central polarity, where the Village is the place a move costs something and the Forest is where it costs nothing; `ch2:167` states it outright. **Fable formula (10)** — five chapters × two, and only the second of each pair carries agency at all, since *Left the Village* is locative and was never a defect. **Mythic counterpart (2)** — `ch9:610`, `ch9:672`. So the arguable set was five headings of one formula, not sixteen. The book had also already answered it: **ch7 carries no village heading, and ch9's own fable headings dropped the word** — *Why the Player Left (And What They Carried With Them)*. | **Wendell 2026-08-03: "keep all 16, log the ruling."** Headings are display type, not prose claims, and are **out of scope for the agency audit at every grade**. The reason generalises past the village: **a heading names the pattern, the body names the people** — which is C1's own formulation, that patterns don't teach, people do, and the pattern is what their teaching leaves behind. A signpost at pattern altitude over a claim at person altitude is the model working, not a mismatch. | A heading stops reading as an unfinished conversion, and the next abstraction to reach a heading is already ruled. | **RULED 2026-08-03 — 0 of 16 converted, no prose changed.** Recorded as a `HEADING` / `DISPLAY-REGISTER` entry in the registry's `registers` block with `scanned: false`. `agency_grep.py` already skipped lines beginning with `#`, so this makes existing behaviour intentional rather than incidental. Closes the heading keep-candidates held in `R8_TEST_CH9.md` and `R8_TEST_CH4.md`. **`ch9:690`, the book's last sentence, is NOT a heading and remains open** — the same reasoning points at keeping it, but it was not part of this ruling. |
| **DL-39** | `manuscript/ch9.md:690`, the book's last sentence | The last open village site, and the only one where the cost of R8 is entirely in register rather than grammar. *"Now go build yours: the village is waiting."* Its twin, the `ch9:672` section heading, closed under DL-38; this did not, because it is prose and the heading ruling did not reach it. | `R8_TEST_CH9.md`: pluralised it is clean grammar — this was never in the BREAKS bucket — and 7→8 syllables, same beat count, same stress pattern. The cost is that *"the village is waiting"* closes the book on a singular, patient, almost-personified figure, matching the singular *you* it is spoken to and the singular *the Player* that carried the whole chapter. One figure addressing one figure, at the exact moment `VOICE_ANCHOR.md` says matters most: *"the closing move is the register… An edit that tidies a paragraph and loses its last beat has failed this anchor."* *"The villagers are waiting"* is arguably the more truthful sentence and is a different last beat, not a cleaner version of the same one. | **Wendell 2026-08-03: keep.** The sentence ships as written. With DL-38 this closes the whole village question: every site in the book is now converted, ruled keep, or ruled out of scope. | The book ends on the beat it was written to end on. | **RULED 2026-08-03 — no change made.** `R8_VERDICT.md`'s held-for-Wendell item 1 closed in place. Recorded against `exceptions` reasoning rather than a new register: this is one sentence ruled on register grounds, not a class. |
| **DL-40** | the six Faces and the Player, book-wide; `instruments/agency_registry.yaml` Grade 1 | **The Faces were in no grade at all.** `R8_VERDICT.md` held the question open as *"Are the Faces Grade 2 or Grade 6?"* — Grade 2 licenses named-part personification, but every uncontested Grade 2 use is *"a part of me"*, interior to one person, while the Faces act externally on a population with independent biographies. Neither branch fit, and R8 could not apply either way. | Measured 2026-08-03: `the Shaman`/`Challenger`/`Regent`/`Architect`/`Diplomat`/`Sage`/`Player` resolve to **no grade** in the registry — only `Diplomat shadow` was listed, at Grade 2 — so **127 Face-subject instances across 69 distinct verbs had never been scanned by anything.** The usage splits two ways and both are licensed: **fable character** (*"the Shaman lived in the village"*, *"the Regent built walls instead"*) and **role-generic** (*"the Regent operates through tradition"*, *"Here the Shaman stops sensing and starts doing"*). | **Wendell 2026-08-03: Grade 1.** The answer to Grade-2-or-Grade-6 is neither — **the Faces are role nouns**, and a role noun takes the verbs of whoever fills the role, as *the surgeon operates* does. The book names the occupant at `ch9:51`: *"you don't **become** one of them: you **move through** all of them."* C1 is satisfied directly, the mechanism being the person in the seat — the reader, a named Head, or a fable character. **One constraint keeps it from being a blank cheque: the occupant must be recoverable.** A Face acting where nobody could fill the seat is the flag; that is the test, not a verb-class list, since Grade 1 licenses every class anyway. | A whole entity class stops being invisible to the audit, and the census's *"you cannot pluralize the Regent"* puzzle is explained rather than parked. | **RULED 2026-08-03.** Seven entities added at Grade 1 with the reasoning in `constraints`. **0 Face findings, as expected** — but total findings fell **414 → 355**, because the net breaks a match when a Grade-1 subject sits between an abstraction and its verb, so registering the Faces removed **59 false positives on other subjects**. `Diplomat shadow` stays Grade 2 — a shadow is an interior distortion, a different thing from the Face. One strain recorded: `ch1:193`, *"the WAVE, which the Shaman hands you in Chapter 3"*, the only Face acting on the reader from outside the frame; licensed, not a defect. Closes `R8_VERDICT.md` held item 4. |
| **DL-41** | `instruments/agency_registry.yaml` `meta.outranked_by` and Grade 2 | **The registry deferred to a retired file and had the daemon roster wrong in both directions.** C2 named `DAEMON_CANON.md` as canon outranking the registry, and Grade 2's constraint — *verbs stay inside each daemon's canonical job* — deferred to it for the job list. The registry carried it as `MISSING FROM REPO` since it was built. | Measured 2026-08-03. **It is not missing, it is retired**: `MANUSCRIPT_FILE_CANON.md:102` lists `claude/DAEMON_CANON.md` under *"Docs that are stale and will mislead you"*, carrying *"the retired 8-gate walk, the retired four-stage sequences, and the retired 'jeppi' naming."* Deferring to it would have reintroduced three retired systems rather than failing silently. Against the live roster at `ch2:270`–`420`, Grade 2 was **missing three of the seven daemons** — Controller (18 uses, the most-used daemon in the book), Skeptic (14), Fixer/Healer (10) — all invisible to the recall net at every grade. The Fixer's absence was the sharpest: it carries `VOICE_ANCHOR.md` anchor 4, *"The Fixer is happy to take him"*, which that document names as **the model of licensed interior agency**. The registry's own exemplar was not in the registry. It also listed **Vulnerable Child as a roster member**, which `MANUSCRIPT_FILE_CANON`'s structural facts contradict: *"Chapter 2's daemon roster carries seven, not eight — the Vulnerable Child left with the gate walk."* | **Wendell 2026-08-03: do all three.** (1) `DAEMON_CANON.md` **dropped from `outranked_by`**, replaced by the live sources — `manuscript/ch2.md` and `MANUSCRIPT_FILE_CANON.md`. Canon points at the manuscript, not at a retired planning doc. (2) **Controller, Skeptic and Fixer/Healer added** at Grade 2, and a `roster` block added quoting each daemon's stated Job from ch2, so *"verbs stay inside each daemon's canonical job"* finally has a live source. (3) **Vulnerable Child keeps Grade 2 but is re-traced** — it is the part the Seven form around, not one of them (`ch2:384`), so the per-daemon job constraint and the two-routings inflation rule do not apply to it. | Grade 2 stops deferring to retired canon, the three heaviest daemons stop being invisible, and the job constraint becomes checkable. | **RULED AND APPLIED 2026-08-03, no prose changed.** Findings 355 → 368. **All 13 new findings are a registry fault, not defects** — see the two structural faults now flagged in Grade 2's constraints: **(a) `somatic` is named in Grade 2's `verb_classes` but is not a defined verb class**, so the embodied-three rule has never been machine-checkable; **(b) Grade 2 licenses the three highest-severity classes and none of the low-severity ones**, so a daemon may think and speak but flags for moving — *"the Fixer turns I might not be equal to this into a project plan"*. Grade 3 has the same shape and symptom (17 mechanical findings on channels). Both flagged for Wendell, not fixed: they are amendments, not corrections. |
| **DL-42** | `instruments/agency_registry.yaml` Grades 2 and 3 | **Both grades licensed the dangerous verb classes and flagged the harmless ones.** Grade 2 licensed perception, intention and speech — the three highest-severity classes — while mechanical, directional and causal-bare were absent, so a daemon could think and speak but flagged for moving. Grade 3 had the same shape. Surfaced by DL-41: adding the three missing daemons produced 13 findings, every one of them this fault. | Measured 2026-08-03 before the change: **Grade 2 carried 34 findings and 33 were mind-free classes** — *"the Controller routes coordination"*, *"the Fixer turns I might not be equal to this into a project plan"*, *"the Healer costs you more"*, *"the Skeptic is what makes you pressure-test it"*. The single social-causal finding was a false positive (*conditions* as a noun in *"guards the conditions a group needs"*). Grade 3 carried 83, of which 37 were mechanical or causal-bare, including *"the charge accumulates"* — which is exactly what a current does. The taxonomy's own notes already said so: mechanical is *"mind-free by definition"*, causal-bare is *"most forgivable"*. | **Wendell 2026-08-03: apply the a fortiori principle to grades 2 and 3.** An entity licensed for a high-severity class is licensed for the **mind-free** classes as well — mechanical, directional, causal-bare, pattern — because if it can think it can move. **The boundary is not strict severity ordering.** A naive a fortiori from perception (severity 1) would license every class including **social-causal**, and that is wrong: social-causal needs a counterparty and a will directed at another party, which is an *additional* claim rather than a weaker one. Nothing about being able to think implies it. **This is the same line E-1 draws for the channels** — delivery of signal versus exertion of pressure — and it holds here for the same reason. Social-causal and speech stay restricted at both grades. | The table stops contradicting itself, and social-causal stays narrow enough that E-1 remains a real exception rather than a formality. | **RULED AND APPLIED 2026-08-03, no prose changed.** Findings **368 → 298**. Grade 2 **34 → 1** (the known false positive). Grade 3 **83 → 46** — what remains is 24 intention, 17 social-causal, 5 speech, which is the material that should be read. Closes flag (b) from DL-41; the original wording is kept in the registry beneath the ruling because it is the evidence. **Flag (a) stays open: `somatic` is still named in Grade 2's verb_classes and is still not a defined verb class**, so the embodied-three rule remains unenforceable. |
| **DL-43** | `the field`, Grade 6; `instruments/agency_registry.yaml` and `agency_grep.py` | **The field was filed with the leftovers.** Grade 6 is *"residue verbs, no mind, no social act"* — the shelf for *the culture*, *the game*, *the system*, things that are what people's action left behind. But `SPEC_FIELD_OF_BODIES_2026-08-02` made the field **physical**: generated by the bodies present, existing between them and nowhere else, gone when they leave. `ch7:359` now reads *"Everyone there makes it."* A thing like that pushes and carries the way a magnet pulls — without a brain. Every force verb on it was flagging. | Read 2026-08-03: **14 candidates on `the field`, and they split three ways.** Seven are field physics — *"Take the care out and the field carries nothing"*, *"The field will push for one more round"*, *moves*, *fills*, *runs*, *turns*. Three are mind verbs — *the field **learns***, ***allows***, ***will let** you*. Two are speech — *what the field **asks***, *let the field **answer*** — plus *makes space* as a borderline third. | **Wendell 2026-08-03: the narrow version, six verbs only.** The first proposal was to license `directional` + `mechanical` outright, roughly forty verbs. **He rejected it on his own W-1 precedent — license `want`, not the intention class — and was right twice.** So: `carry`, `move`, `push`, `fill`, `run`, `turn`, the six that actually appear on the page, licensed on `the field` **alone**. A seventh force verb in new prose flags and somebody looks at it, which is the point — *the sentence where a field pushes is the sentence most likely to be hiding a person who pushed*. Perception, intention, speech and social-causal stay barred at every strength. **Not a grade-wide license:** the rest of Grade 6 keeps the pattern-only ceiling, because *the culture* has no bodies generating it. | The frame's own physics stops reading as a defect, without buying it by switching off the tripwire on the sentences that hide people. | **RULED AND APPLIED 2026-08-03, no prose changed.** New `entity_partial` mechanism in the registry — a license attached to one named entity inside a grade rather than to the grade — and `tier()` taught to take the subject, which it had never received. Findings **109 → 102**; `the field` **14 → 7**. Verified by construction: *the field pushes* clears, *the culture pushes* still flags, *the field learns* still flags. **The seven real sites remain open** and are the next prose block. |
| **DL-44** | the six Schools; `instruments/agency_registry.yaml` Grades 1 and 6 | **`Heads` has been a Grade 1 entity since the registry was built, while the Schools they head sat at Grade 6** — the people licensed, the institution they run not. Grade 6's ceiling is pattern-residue verbs, so every School construction in the book was a violation by the table. | Measured 2026-08-03: **twelve constructions across five chapters** — *teaches* ×5, *takes* ×2, *makes*, *attracts*, *applies*, *replaces*, *exists*, *stands*. **All twelve were invisible to the recall net**, because a registry entity that is a PREFIX of a longer proper name breaks the intervening-phrase filter: the registry listed `the school`, the book writes *"The School of the Pattern teaches"*, and `of` triggered the break. Found by broad sweep during the pattern block, not by the net. | **Wendell 2026-08-03: Grade 1.** An institution noun takes the verbs of whoever staffs it, as *"the university announced"* does — the Faces argument one step out, and stronger here, because the staff are **named, titled, and sign the chapters**: Maera Voss (Body), Corin Ash (Line), Sera Quill (Oath), Irix Vale (Pattern), Elian Cross (Bridge), Thalen Orr (Horizon), each signing *"Head of the School of the X"* beneath their treatise. So *"The School of the Pattern teaches in language that has a bad record"* is Vale and the faculty teaching, and C1 is satisfied more explicitly here than for any other entity in the book. **The constraint, matching the Faces: the Head must be recoverable.** | The largest unread block in the book turns out to contain no defects, and the reason is recorded so it is not re-opened. | **RULED AND APPLIED 2026-08-03, no prose changed.** `the school` moved out of Grade 6; all six full names added at Grade 1 in their own right rather than relying on the `the school` prefix, which is what resolves the net's blindness to them. Findings unchanged at 39 — the twelve were never counted, and are now licensed rather than hidden. |
| **DL-45** | `open_rulings: W-3`; `exceptions: E-1`; four sites in ch1, ch5, ch9 | **W-3 asked where to cut the agency audit for print** — its proposal was *"perception + intention on unlicensed subjects in the nine shipping chapters; all else parked to edition two."* It was written to triage a backlog. | Read 2026-08-03, all 39 findings then standing: **18 Tier 3, 15 Tier 2, 6 Tier 1 — and eight were real.** The rest were recorded keeps or false positives (`including` read as social-causal; *"the burnout claims in this book"* where *claims* is a noun; *"the game system Wendell built"* where Wendell built it). **Four of the eight were Grade 7 stuck states speaking**, in a construction identical to the one E-1 protects at Grade 3 — the registry was calling *"Sadness says"* thesis and *"The shame says"* a defect, and `ch5:376` runs the device as a matched pair one line apart: *"The shame says: this wound was done to me, therefore I am broken. The gift says: I know how to recognize it and stop it."* | **Wendell 2026-08-03: extend E-1 to Grade 7, and close W-3.** No cutoff — there was nothing left to cut, and eight sites is an afternoon rather than an edition. **E-1 now covers Grade 7** (shame, guilt, anxiety, dread, worry, despair, resentment, contempt, burnout, the freeze, the shutdown, the stuck) on the unchanged boundary: delivery of a true content about the bearer's own state stays licensed; exertion of pressure on another party stays illegal. `tell` added to E-1's verb list — the original three were read off the ch3 block, which happens not to use it, so the list was a census of one passage rather than of the device. | The audit reaches print without a triage tier, and the register that carries the book's thesis is protected wherever it appears rather than only where it happened to be looked at first. | **RULED AND APPLIED 2026-08-03.** Four cleared by the E-1 extension. Four converted: *frameworks teach → pick*; *the organizations that used tradition to protect power → the people who*; and `LEDGER_CH9`'s own R4 and R1, drafted when that ledger was written and unapplied until now — *what the culture taught me to do → what every teacher, workshop, and well-meaning friend had told me to do*, and *The culture never tells you this → Nobody tells you this*. Findings **39 → 29**. Tier 3 **18 → 11**. **W-4 is the last open question.** |
| **DL-46** | `manuscript/ch9.md:247` and `:419`; `agency_registry.yaml` Grade 6 `entity_partial` | **Two conversions made under DL-45 were reverted the same day.** `LEDGER_CH9`'s R4 and R1 had been drafted when that ledger was written and applied for the first time closing W-3. | *Then I did what **the culture taught me** to do: I looked outward for the solution.* · ***The culture never tells you this**: the Founder move probably already sits somewhere in your history.* | **Wendell 2026-08-03: "keep these and lose the rest."** Both ship as written. My reading of why, recorded as mine rather than his: **R4 replaced *the culture* with *every teacher, workshop, and well-meaning friend* — inventing a specific roster where the original was honestly general.** DL-36 names that exact failure — *fabricated specificity*, *"you cannot make a hypothetical concrete by adding details"* — and closes on *"vagueness admits what it is; a definite article over a blank does not."* R1's *Nobody tells you this* is the milder loss: it drops the source of the silence, and naming the source is the sentence's point. | The audit stops trading a true general statement for a false specific one. | **APPLIED 2026-08-03.** `the culture` licensed for `teach` and `tell` via `entity_partial`, on the Schools mechanism — a culture transmits through the people carrying it, and at `ch9:247` the transmitters are the paragraph's own subject (*"I came to it the way most people come to it"*). Still barred: intention, and every social-causal verb but `teach`. **W-3 therefore closed at two conversions, not four**, plus the four E-1 carries. Findings unchanged at 29. |
| **DL-47** | `manuscript/ch3.md`, Move 5 · Say the Thing Under the Thing; `open_rulings: W-4` | **W-4 asked where to place a beat showing systemic language as the place a confrontation gets avoided** — *"the culture here is toxic"* said instead of *"Dave, you interrupted her three times."* | **The proposal's vehicle does not exist.** `The Game So Far` has zero hits across nine chapters (`SPEC_REPETITION_AND_CUTS.md:140`). **The demonstration already exists, unnamed**, at `ch6:122` — *"They could then dismiss structural insight with the vocabulary itself: we already looked at the incentives, we're addressing the leverage points."* ch3's council parable and ch7's letting-agent example are the same genus. **Three demonstrations, zero labels.** A 6 Game Master read produced six positions; three argued against a new name at all. | **Wendell 2026-08-03 took the Regent's: "do the regent's."** The book has already sworn to a move that does this job — `Say the Thing Under the Thing`, a named game move at `ch3:850`, in the roster the reader carries into every chapter after. Minting a second name for it is how a roster becomes a glossary. **No new move, no new name, no new slot** — one paragraph added to Move 5's `In practice`, which already teaches the first way a read dies (the case). This is the second way. | The beat lands without spending vocabulary the reader is already carrying ten moves' worth of. | **RULED AND APPLIED 2026-08-03.** Drafted under the `mtgoa-review` pass: ELI5 written first, three candidate versions measured. v1 scored `copula 2.29`/`be 1.37` heavy; v3 tripped `empty 1.43` on *the second way*; the shipped version reads **be 0.75, copula 0.00, waste 0.34, empty 0.00, head 0**, gate and voice clean. The example pair is W-4's own, verbatim, so nothing was invented to fill it. **What the ruling declines, recorded:** the evidence says the Architect supplies the vocabulary and the Diplomat reaches for it, which made the Ch6→Ch7 seam the natural home — placing it on a Shaman move costs that reading. **W-4 was the last open question; the registry now has none.** |
| **DL-48** | `manuscript/ch1.md:269`; `back_matter/about_the_author.md`; `back_matter/enrollment.md`; `instruments/build_book.py` spine | **The last four ship blockers, and three of them were already solved on a branch nobody had read.** `shipcheck.py` ranked them: app routing (1 site), placeholders (1 site), build gaps (2 components). | `ch1:269` read *"Find the hundred-and-twenty-card deck … at **[ URL / QR ]**"* — a live placeholder that typesets verbatim. `about_the_author.md:6` read *"the builder of bars-engine, the app this book routes to"* — the routing blocker DL-20 ranks first, pointing a reader at a product not shipping with the book, and it also said *"certifies others to run them"* against `8300305`'s removal of certification. `back_matter/enrollment.md` did not exist. **`claude/book-pdf-epub-production-ybxa11` carried all three**, in `ca05457` and `e9d8837`, and its own commit message had already made the connection this branch had not: *"ch1's [ URL / QR ] resolves to masteringallyship.com, where the deck lives, so the last placeholder and the enrollment page closed on one decision."* | **Wendell 2026-08-05: *"take the placeholder and the app-routing line. We're not doing kickstarter backer page, but we are doing enrollment."*** Taken **by file, not by merge** — that branch has 91 commits this one lacks and this one has 115 it lacks, against a merge-base of `4f1a4e2`, so a merge would have dragged an unread production tree through an agency audit. Two files copied at their tip, one line edited, one spine level changed. **Kickstarter backers GAP → OPTIONAL**, using Wendell's own wording from that branch on 2026-08-03: *"we're removing kickstarter backers as necessary for this build."* The component stays on the spine with the ruling recorded beside it, so raising it back when a backer export exists is a one-word edit rather than an archaeology problem. | The book stops pointing at an unshipped app and an unwritten URL, and the reader who finishes ch9 gets handed somewhere to go. | **APPLIED 2026-08-05. `shipcheck.py`: SHIPPABLE, all six categories clear** — first clean board on this branch. `build_book.py` assembles 123,263 words, 47 marginalia blocks, no BLOCKER or GAP missing. Both incoming files run through the `mtgoa-review` pass: enrollment **be 0.46, copula 0.47, waste 1.29, zombie 0.78, empty 0.62, head 0**; about the author **be 0.66, copula 0.69, waste 0.69, empty 0.39, head 0**; gate and voice clean on both. Book-wide review: seven of seven `ok`, the two standing heavies unchanged (`ch7 passive 1.41` DL-34, `ch1 inchoative 1.34` DL-35). **One thing flagged and not fixed:** `acknowledgements.md:11` still reads *"Their names are on the page that follows"*, a live forward reference to the page now OPTIONAL. Under OPTIONAL it is a deferral rather than a dangle, and rewriting it is a prose call on Wendell's own acknowledgements, so it waits for him rather than for me. |
| **DL-49** | `back_matter/acknowledgements.md:11`; `instruments/build_book.py` spine and level rationale | **The forward reference DL-48 flagged, and the page carrying it.** `acknowledgements.md:11` read *"The backers who funded it. **Their names are on the page that follows**, and the book exists because they paid for it…"* — a live pointer to `kickstarter_backers.md`, which DL-48 had just ruled would never be written. | The pointer had been load-bearing in the build for weeks: `build_book.py`'s spine comment cited it as the reason the backer page *"has to sit immediately after them"*, so a sentence in Wendell's prose was pinning a component's position. | **Wendell 2026-08-05: *"fix the acknowledgements line, drop the forward reference. We're also cutting acknowledgements this version."*** Eleven words out and nothing else touched — *"Their names are on the page that follows, and the book exists"* → *"The book exists"*. The thanks to the backers survives intact; only the pointer goes. Then the page itself came **off the spine**, on the Key Terms glossary's precedent: on disk, correct, unbuilt. | The book stops pointing at a page that will not exist, and this edition ships without a section Wendell has not finished thanking people in. | **APPLIED 2026-08-05.** Prose: `copula 0.46 → 0.23`, every other counter flat or down, gate and voice clean. Build **123,254 → 122,988 words**; `shipcheck` SHIPPABLE, book-wide review seven of seven `ok`. **The mistake worth recording, because I made it first:** I set the level to `OPTIONAL` and called it done. `OPTIONAL` governs what the builder does about a file that is **missing**, and this file is on disk — at `OPTIONAL` it kept building, all 266 words of the section the ruling said to cut, and both `shipcheck` and `build_book` reported clean while doing it. **A level is not a decision about whether a page prints; only the spine is.** That is a fourth state the three-level scale cannot express — not required, not noticed-if-absent, not allowed-to-be-missing, but ruled out of this edition — and it is now written into the constants comment where the next reader meets it. |
| **DL-50** | `front_matter/also_by.md`; `manuscript/ch3.md` §4; `instruments/citation_audit.py`; `instruments/index_build.py`; `back_matter/index.md`; `build_book.py` spine | **The three components DL-49's audit named as never listed, plus the borrowed-term debt.** `citation_audit.py` stood at 9 findings: 8 BORROWED, 1 DEAD (Peter Levine credited in Appendix G and used nowhere). | **Six of the eight borrowings were already paid.** The book's convention is one attribution note per chapter, placed where the reader starts using the tool: `ch6:238` for Meadows (*"this chapter uses it in her sense throughout"*), `ch8:295` for Laloux, `ch3:431` for wu xing, `APPENDIX_F:8` for Johnson. The audit's rule was *owner within 400 characters*, so every one of those chapters read as uncredited from the second mention onward — `leverage point` reported sixteen times in a chapter that names Donella Meadows and explains her claim. **Two were real:** Gendlin appeared in no chapter at all despite eight uses of *felt sense*, and Johnson was credited only in Appendix F, never in ch3 where the Polarity Map is taught. **And Levine's DEAD credit had a home already written** — `ch3:197` makes his claim in the book's own words (*"To let your body complete the cycle it needs to complete"*) without naming him. | **Wendell 2026-08-05: *"do the also by page, lets cite the borrowed terms, and look for a place to insert Peter Levine's work ad an index would be useful."*** One paragraph at `ch3:253`, head of Section 4, on `ch8:295`'s precedent — the chapter's practice section, before the reader uses the borrowed tools. It pays all three debts at once and says what the book does **not** claim: *"He works with trauma, where the jam runs deep enough to want a trained person in the chair with you; this chapter practices on the everyday version, and I answer for borrowing it down a level."* The audit's scope changed from a 400-character window to the file. The wu xing row came out entirely — it asserted Ted Kaptchuk owns a two-thousand-year-old tradition, which is like requiring a translator's name beside *Tao*. | The lineage promise in Appendix G — *"where an idea here is load-bearing, it has a lineage"* — holds in both directions, and the audit reports defects instead of reporting its own rule. | **APPLIED 2026-08-05.** Citations **9 → 2**, DEAD **1 → 0**. The two remaining are inheritance cases (ch3 credits both; ch4, ch5, ch8 and the appendices use them as taught vocabulary) and stay reported, because BORROWED is marked judgement and silencing a judgement call to make a board go green is the failure this log exists to prevent. Prose drafted under `mtgoa-review`: v1 scored `be 1.78 / copula 1.96 / passive 2.22` heavy, measured against `ch8:295` itself at **be 0.69, copula 0.00, passive 0.00** — which proved heavy was drift and not the register. v2 measured clean and still carried two slop patterns a counter cannot see, both caught by a detect-only `/no-ai-slop` pass and fixed on Wendell's instruction the same day. **A colon reveal** — *"comes from Eugene Gendlin's* Focusing*: the body's knowing that arrives before you have words for it"* — noun phrase, colon, lowercase gloss, now a plain clause. **Synonym cycling**: five figures for one relation in seven sentences (*came from · supplies · belongs to · carry their own debt · the note that pays it*), two of them metaphors the sentence did not need. The fix is the rule's own — repeat the clear word rather than rotate for style — and the model is `ch8:295`, which uses exactly two forms, *come from* and *is X's*. This note now uses the same two. v3 ships at **be 0.64, copula 0.43, passive 0.00, waste 1.26, head 0**, gate and voice clean. Its one em-dash put ch3 over budget; the budget only ratchets down, so the sentence was rebuilt rather than the budget raised. **`front_matter/also_by.md`** added at the card page, verso of the half title, listing *Igniting Joy* — named in three shipping components and on no page. **`back_matter/index.md`**, 86 entries, generated by `index_build.py`. Build **122,988 → 124,158 words**; `shipcheck` SHIPPABLE, review seven of seven `ok`. |
| **DL-51** | `instruments/index_build.py`; `back_matter/index.md`; `manuscript/ch4.md` (flagged, unedited) | **The index had to be generated, because a hand-typed one goes stale the first time a section moves and points a reader at a section the term has left.** A print index points at page numbers; this book has none until a typesetter gives it some, so locators are **chapter and section**, the one address that survives typesetting. | The build enforces two rules and aborts rather than emitting: **no entry without a hit**, and **no primary locator that no longer holds its term**. Both fired on real defects during the first three runs. | **Design ruled here, recorded because the failures are the argument for it.** Rule 2 caught my own judgement twice: I recorded the five Ups as taught at `ch3 §4` and the build refused, because §4 runs them bare (*Stage 1: Wake*) and the two-word forms are the move names in §6. Rule 1 aborted on **`School of the Line`** — and the abort was right about the book while wrong about the index. | An index that cannot drift, and a term list that cannot contain a page-reference to nothing. | **APPLIED 2026-08-05. 86 entries across nine groups**, `--check` clean. **The finding worth Wendell's attention, flagged and not fixed:** all five mentions of the Challenger's school live in **margin notes**, and the shipping body text names it **zero** times — against School of the Body 8, Pattern 4, Bridge 4, Horizon 3, Oath 1. Every other school is named in the prose of its own chapter; ch4's is named only by characters in the margins. That is a question about ch4's prose and not a reason to leave a school out of the index of a book that prints its name five times, so the index counts marginalia — the one instrument here that does, since margin notes are printed and a reader looking a term up does not care which voice said it. **Second self-inflicted defect, recorded:** putting the index on the spine made it one of its own surfaces, so every term matched itself and `--check` reported STALE on a file it had just written. Excluded by path. |
| **DL-52** | `manuscript/ch4.md` §3 · The Concept | **The Challenger's school was named nowhere in the shipping body text.** Found by `index_build.py`'s no-entry-without-a-hit rule (DL-51), which aborted on `School of the Line`. Body-text census: Body 8, Pattern 4, Bridge 4, Horizon 3, Oath 1, **Line 0** — all five mentions inside margin notes, spoken by characters. | Every other school gets named in the prose of its own chapter, and three of the five sit in **Section 3: The Concept**, doing positioning work rather than label work: *"The School of the Pattern teaches in language that has a bad record"* (`ch6`), *"The School of the Oath is not a defense of the people who used tradition to protect power"* (`ch5`), *"The School of the Body is asking for the trust"* (`ch3`). ch4 §3 is ch6 §3's structural twin, down to the shared subtitle *(And Why It's Not What You've Been Told)*, and it was the only one of the six with an empty slot. | **Wendell 2026-08-05: *"fix the school of the line in ch4."*** One sentence, on ch5's negative form, at the pivot where the section turns from what the villagers hear to what the move actually is: **The School of the Line will not turn you into an asshole.** **Wendell revised both halves of my draft the same day**, and each correction has a name. My *does not teach you to be harder* drew *"vague. What does it mean to be harder? It would be better to say it's not going to turn you into an asshole."* — a comparative with no referent, which is the abstraction the book's own empty-head rule is about, and the plain word was sitting right there in the paragraph above (*"you've probably met them"*). My second sentence, *The clean no is something different*, drew *"throat clearing. cut it."* It was, and cutting it let the profanity land directly against *you've probably met them* instead of through a buffer. **This is the book's first profanity**, which is Wendell's call as the author and is recorded here so nobody sands it to *unkind* on a later pass. It names the school while doing the section's own work — the misreading ch4 §3 spends three paragraphs refuting is precisely that a clean no means aggression — so the name arrives as argument rather than as signage. | A reader who wants the Challenger's school can find it in the book, and the index entry points at prose instead of at a margin note. | **APPLIED 2026-08-05.** Body-text hits **0 → 1**; all six schools now named in their own chapters. Measured at section scope rather than sentence scope, because a 37-word sample scored heavy on four counters that belong to the two shipped sentences beside it — ch4 §3 whole reads `be 0.92 → 0.88`, `copula 0.75 → 0.67`, `empty 1.18 → 1.05`, every other counter flat, and the standing `expletive 2.03` is pre-existing and unmoved. Cutting the throat-clearing is what moved the counters; the first draft held them level. `index_build.py --check` clean at 86 entries, `shipcheck` SHIPPABLE, review seven of seven `ok`. **Worth recording about the instrument:** this is the second defect in two days found by a build that refuses to emit rather than one that reports. `citation_audit` had reported the same class of gap for weeks as a number nobody had to act on. |
| **DL-53** | `manuscript/ch3.md` §4 borrowing note; `back_matter/enrollment.md` Speaking | **A second `/no-ai-slop` pass on prose that had already been through one, and the pass found the thing the first one missed by looking at the wrong scale.** DL-50 fixed a colon reveal and synonym cycling *inside* the note. | **The whole note was synonym cycling one level up.** Its opening was `ch8:295`'s sentence with the words swapped — *"Three of the **maps** in this chapter **are not mine**, and you should know whose they are before you **use** them"* against my *"Several of the **tools** … **came from somewhere else** … before you **run** them."* Three more: `want` on an inanimate subject (*"the jam runs deep enough to want a trained person"*), a near-duplicate of `APPENDIX_F:8` (*"his own name for it is polarity management"* against *"His term is polarity management"*), and a pun (*"what the read in this chapter reads"*). | **Wendell 2026-08-05: *"run the slop pass on the enrollment page fixes AND on the note."*** The note rewritten in its own words. **On the enrollment page, two of my three earlier findings were withdrawn**: *"one person and not an audience"* is a faithful quote of `ch9:526` and editing it would have broken the citation, and *"A level sits above that one"* is the book's altitude vocabulary rather than a fake-strong verb. | A device the book uses four times reads as a device instead of as a copy, and a detect pass stops generating edits it cannot justify. | **APPLIED 2026-08-05.** Note ships at **be 0.57, copula 0.86, waste 1.14, passive 0.00, inchoative 0.00, head 0**. **Two intermediate drafts failed, both in the shape `mtgoa-review` predicts**: fixing the inanimate `want` produced *"the jam goes deep"* — `inchoative 22.22`, the launder pattern verbatim — and *"the work belongs with a trained person"* tripped `empty_head.py`, because **the work** is on that list. Fixing one counter moved the defect to a counter nothing was watching, twice in four minutes. **The real find was not slop at all.** `enrollment.md` read *"I said in Chapter 1 that a move like that asks for far more and needs a history behind it."* **That passage is in the source branch's ch1, not this one** — theirs 8,300 words, this branch's 7,437. DL-48 took three files across a 91/115-commit divergence and checked none of their internal cross-references against this branch's chapters, which is the specific risk of taking by file that I named and then did not act on. Attribution dropped, claim kept. **Open for Wendell:** porting the ch1 passage instead, which sits inside an 863-word divergence and is not a decision to make blind. |
| **DL-54** | `manuscript/ch3.md` §4 borrowing note; `back_matter/enrollment.md` Speaking | **Three reader questions the counters could not ask, and two of them trace to the same cause as DL-53.** | *"and I answer for borrowing it down a level"* — **Wendell: *"no idea what this means. How does one borrow something down a level. From what level to what level?"*** It is `ch8:295`'s phrase, where it is exact: Laloux's *evolutionary purpose* describes an organization, the book applies it to a person, and organization→individual is a level. **Levine is not a level shift at all.** He studies severe jams; the book works ordinary ones. That is severity, not altitude, and the phrase arrived attached to the sentence pattern DL-53 had already caught me copying. · *"The Polarity Map is Barry Johnson's, and he calls it polarity management"* — **Wendell: *"doesn't explain what it is, just what he calls it."*** True: a name with no content, in a note whose whole job is telling a reader what they are running. · `enrollment.md`: *"A move like that asks for far more, and it needs a history behind it. I have that history."* — **Wendell: *"What does the move ask for? Claiming that I have the history is conceited. Just cite the history."*** | **Wendell 2026-08-05.** The Levine sentence now says the actual relation: *"He studies that jam where it is severe, in trauma, which calls for a trained person in the chair with you. This chapter works the everyday version, a bad afternoon rather than a wound, and the stretch from one to the other is mine to answer for."* Johnson gains his distinction, taken from `APPENDIX_F:8` where the book already states it: *"and so is the distinction it rests on, that some tensions are problems you solve and others are pairs you hold."* The Speaking paragraph names what the move asks for and replaces the claim with the record: *"That asks for somebody the group will keep listening to after the temperature changes. I ran the first Mastering the Game of Allyship course, and the Allyship Dojo has been running these moves in live conditions for longer than the book has existed."* | A reader can check every sentence in the note against something, and the enrollment page earns its ask instead of asserting it. | **APPLIED 2026-08-05.** Note: **be 1.05, copula 1.15, waste 1.14, zombie 1.17, passive 0.00, head 0**, all inside band. Speaking: **be 0.22, copula 0.00, waste 0.40, passive 0.00, head 0**. Both cited facts are verbatim from `acknowledgements.md:19` and `:21`, in Wendell's own words. **Flagged:** DL-49 took the acknowledgements off this edition's spine, so the course and the Dojo are now named nowhere else in the book and the enrollment page is their first and only mention. That is fine for an enrollment page and worth knowing. **The pattern across DL-50, DL-53 and DL-54:** every defect in this note came from writing it beside `ch8:295` instead of from the material. A model is for the shape of a passage. Borrowing its sentences carries its arguments too, and those arguments were about other sources. |
| **DL-55** | `manuscript/ch1.md` — `## The Trust System — What They Decide`, ported from `claude/book-pdf-epub-production-ybxa11` | **The book had two of its three systems.** ch1 taught the Token System (what you spend) and the Ticket System (what you earn) and then went straight to *Why a Game*. The third beat, the Trust System (what they decide), existed only on the production branch — 753 words, and the largest single piece of the 863-word ch1 divergence DL-53 turned up. | **`master` is not the trunk and was checked first.** It has no `manuscript/` directory, its last commit is 2026-06-09, it shares **no merge base** with this branch, and its chapter numbering is off by one from current canon (*"Ch2 SHAMAN"* where the Shaman is ch3). It is the retired `chapters/` lineage. **The live divergence is two branches, and it is smaller than the word counts suggest**: structurally it is two sections — this one, and ch2's *A Second Walk, Through Somebody Else's Forest* (1,380 words). This branch's own +2,056 across ch3/5/6/7/8 is the agency audit, additive and structurally invisible. | **Wendell 2026-08-05: *"port the trust system section."*** Placed between the Ticket System and *Why a Game*, which is the slot the three-system sequence had left empty. Ported verbatim — it is Wendell's prose and the pass audits it rather than edits it. | The Arcade's economy is complete, and the reader learns that some doors are shut regardless of what they brought. | **APPLIED 2026-08-05.** Audited against this branch's instruments, which its own branch never ran: gate clean, voice clean, `be 0.90, copula 0.87, waste 1.04, zombie 0.18, passive 0.86` all in band. ch1 **7,437 → 8,198 words**, build **124,158 → 124,957**. **Side effect worth having: `ch1 inchoative 1.34` cleared** — DL-35's standing heavy, now 1.22, so the book-wide diet drops from two heavies to one (`ch7 passive 1.41`, DL-34). `shipcheck` SHIPPABLE, `index_build --check` clean at 86 entries. **Two things flagged and not fixed.** (1) **An uncredited term of art.** *"Capability, integrity and benevolence, if you want the words for them"* is the three-factor trust model; Appendix G does not carry it and neither does the copyright page. `citation_audit.py` cannot see it — its UNCREDITED check keys on names beside claim verbs, and this passage names no one. Adding a credit means asserting a source, which is Wendell's call. (2) **A forward reference that resolves in substance only.** *"You will find out who those bouncers are in the next chapter"* — **neither ch2 carries the word `bouncer`**, on this branch or the production branch, so this was already loose where it came from. ch2 §6's daemons are the bouncers, and the Protector's Job (*"To keep you alive"*) is the same mechanism, but the word never returns to collect the promise. **Three empty-head hits were considered and kept**: *"things you do / a thing you are"* is the section's own distinction, *"that thing"* is the bouncer deliberately not seeing a person, and `move` is house vocabulary the SOFT list does not know. |
| **DL-56** | `manuscript/ch2.md` §7 — `### *A Second Walk, Through Somebody Else's Forest*`, ported from `claude/book-pdf-epub-production-ybxa11`; `instruments/index_build.py` | **ch2 taught the reader to walk their own Forest and never taught them to walk anybody else's.** §7 held *A First Walk* and went straight to *Try It Now*. The second walk — 1,304 words, the whole of ch2's 862-word divergence and then some — existed only on the production branch. | **Its closing paragraph was already here without it.** `ch2:401` carried the Gerard Egan credit — *"The practice format in this book … comes from Gerard Egan's* The Skilled Helper*"* — which is the last paragraph of this section, ported at some point separated from the section it closes. A verbatim port would have shipped the credit twice. | **Wendell 2026-08-05: *"port a second walk."*** Ported lines 381–435 only, stopping short of the Egan paragraph, and placed immediately before it so the section closes the way it was written to. Verbatim otherwise; the pass audits Wendell's prose rather than editing it. | The inner walk gets its outer half, and the book's central claim — that the parts you have not befriended are the ones you misread hardest in other people — finally has the passage that argues it. | **APPLIED 2026-08-05.** ch2 **7,897 → 9,207 words**, build **124,957 → 126,261**. gate clean, voice clean, `be 0.72, copula 0.89, waste 0.93, zombie 0.15, passive 0.25`. **Egan count verified at 1.** `shipcheck` SHIPPABLE, review seven of seven `ok`, one heavy left in the whole book (`ch7 passive 1.41`, DL-34). **DL-55's bouncer flag is answered.** The two ported sections were written as a pair and the callback survives the port intact: `ch1:133` *"It does not weigh you up, and it does not wonder about you. It checks you against the shape"* → `ch2:441` *"It does not think about you; it checks you against the shape."* The word `bouncer` still never returns, but the mechanism does, in the same clause. **`clearance` added to the index** — coined here, four uses, primary `Ch 2 §7`; 86 → 87 entries. **Flagged about the instrument, not the prose:** `empty_head.py` reports 4 HARD on this section and three are `parts`, which is this book's own word for the daemons (8 uses in ch2, plus *"a part of you"* at `:182` and `:334`). That is the rule's own fourth legal case — *canon the book has taught her* — and the counter cannot see it. No carve-out added: a blanket exemption for `part` would blind the counter to the real placeholder use, and the counter is reporting-only rather than a gate. |
| **DL-57** | `manuscript/ch3–ch9`; `instruments/port_clearance.py`; `instruments/index_build.py` | **A three-way comparison against the common base `4f1a4e2` answered the branch question and found an arc I had broken.** Wendell asked what else the production branch held before abandoning it. | **That branch had changed almost nothing in ch3–ch9: four lines per chapter** against this branch's 85–107. But every one of those lines belonged to one of **two designed series.** **Series A** — what each Face means by *good*, one per chapter ch3→ch8, all six. **Series B** — the same daemon in somebody else, one per chapter ch3→ch8: Controller, Skeptic, Fixer, Emotional Body, Victim, Damaged Self. **The seventh is the Protector, missing on purpose** — ch2's Second Walk works the Protector at the door and these six are what stands behind it. Plus one in ch9 extending the idea to the one-person move. **DL-56 ported the Second Walk without them, which left an arc started and dropped**, and `clearance` — the term the Second Walk coins — was indexed the same day as a ch2-local term with four uses. | **Wendell 2026-08-05: *"port all 13."*** Applied by `port_clearance.py`, which verifies every anchor matches exactly once and refuses if a paragraph is already present, so a re-run cannot double-insert. | The book teaches reading your own daemons and then reads somebody else's once per chapter, and `clearance` is the through-line rather than a term used four times and dropped. | **APPLIED 2026-08-05.** Build **126,261 → 126,878 words**. gate clean, voice clean, whole-set diet `be 0.74, copula 0.91, waste 0.92, passive 0.38`; `shipcheck` SHIPPABLE; one heavy left in the book (`ch7 passive 1.40`, DL-34). **Two collisions, both caused by this branch's own work, both handled on the way in.** (1) Four Series A anchors read *"The village never meant for that to happen"* there and *"The villagers never…"* here, because R8 converted them; anchored on the converted form. (2) **ch6's Series A paragraph arrived carrying *"more often than the village believes"*** — a perception verb on a Grade 6 entity, the exact construction this branch converted forty-odd times. Converted to *"than the villagers believe"* in transit. Importing it as found would have re-introduced the violation the whole audit exists to remove, and nothing would have caught it: `agency_grep.py` reports candidates, it does not gate a commit. **`clearance` re-indexed**: `Ch 2 §7` primary, now spanning six chapters instead of one. The index entry written yesterday was true when written and wrong within a day, which is the argument for generating it rather than typing it. **The branch question is closed: `claude/book-pdf-epub-production-ybxa11` now holds nothing this branch lacks.** |
| **DL-58** | `back_matter/glossary.md`; `instruments/glossary_check.py`; `build_book.py` spine; DL-8 | **The book has had no glossary since DL-8 retired `APPENDIX_C_KEY_TERMS.md` on 2026-07-30**, and that retirement is the argument for how this one is built. The old one was not badly written. It was **written once, against a spec, and never checked again**, so it went on defining a book that had stopped existing: the wrong six for *The Face*, *Gate* as one of eight threshold moments, a Vulnerable Child gone from ch2's roster, chapter tags 0-indexed against a Ch0 that is now Chapter 1, and fifty-one references to trigrams and hexagrams the manuscript no longer contains. | **A glossary is the one back-matter component that rots silently.** The index points at locations, so a moved section breaks it loudly; a definition keeps reading fine forever while the chapter underneath it changes. And `AUDIT_GAME_LOOP_CONFORMANCE_2026-07-30.md:77` has carried *"there is no BAR entry in the shipping glossary"* with a recommendation to add one — a live instruction with no target, because **nothing in the repo could see a missing entry.** A gap looks exactly like a glossary that is finished. | **Wendell 2026-08-05: *"let's do the glossary."*** 37 entries, alphabetical, every definition taken from the chapter that teaches the term rather than composed — *"game tape, not journaling"* for BAR, *"every place with an audience"* for the village, *"a part of you that runs in the background, on its own, with no one holding the joystick"* for daemon. Each entry ends in its locator. `glossary_check.py` runs three checks, and **the third runs backwards**: every coined term the index carries must have an entry. Sources are exempt — Appendix G owns the lineage and a second shorter copy would drift from it — and so are the game moves, whose names are already imperatives. | The book a reader consults mid-practice answers in its own words, and the next term that gets coined cannot quietly fail to appear. | **APPLIED 2026-08-05.** Build **126,878 → 128,184 words**; `shipcheck` SHIPPABLE, review seven of seven `ok`, `glossary_check` 0 findings. Prose `be 0.55, copula 0.26, waste 0.84, passive 1.25` — the first draft ran `passive 1.50`, and the lazy fix was a REGISTERS entry, which only Wendell can rule; five definitions were rewritten instead. **Three defects the checks caught in their own first run, all mine.** (1) *Completed states, the five* was **my** coinage, not the book's — ch3:465 says *"Each channel has a completed state"*. Headword corrected; check 1 exists for exactly this and caught it on a glossary written an hour earlier. (2) The locator check searched the headword string, so it failed three entries where the chapter teaches a concept without using the glossary's phrasing — *Allyship domains, the four* is taught at Ch 2 §10 by naming all four and the phrase itself appears only in Appendix A. Now resolves each headword to the index's own tested regex. (3) **A truncation that hid the answer.** Failures printed `sorted(found)[:4]`, alphabetical, which put App A through App G ahead of every chapter and reported *altitude* as **"found in App A, App B, App F, App G"** for a term that is in seven chapters. I chased that as a real finding before noticing the sort. Now ordered by the book and explicit about what it omits. The underlying cite was wrong too: `Ch 5 §5` where the text sits in `Ch 5 §4`. |
| **DL-59** | `appendices/ON_THE_SHOULDERS_OF.md` — new `## On Trust`; `manuscript/ch1.md`; `front_matter/copyright.md`; `instruments/citation_audit.py` | **DL-55 flagged an uncredited term of art in the ported Trust System.** *"Capability, integrity and benevolence, if you want the words for them"* named a three-factor model of trust and named nobody. Appendix G did not carry it and neither did the copyright page. | **Wendell 2026-08-05: *"credit the trust model if you can. We'll have to do research on this. I encountered it in a TED talk I watched in 2015 so I don't remember who I got it from."*** Searched rather than recalled. **The model is Roger Mayer, James Davis and David Schoorman's**, from *"An Integrative Model of Organizational Trust"* (*Academy of Management Review* 20:3, 1995) — ability, benevolence, integrity, and the paper where trusting and being trustworthy stop being one question. **The candidate talks do not match and were checked.** Frances Frei's TED talk is 2018 and runs empathy, logic, authenticity. Onora O'Neill's 2013 TEDx runs competence, honesty, **reliability** — not benevolence. Benevolence is the distinctive term, and it carries the passage's whole argument: *"Whether you meant well."* | **Credit the source, not the route.** A talk neither of us can identify is not a citation, and the reader needs the paper. Three surfaces: a new `## On Trust` section in Appendix G, one sentence in ch1 on the book's own refrain (*"The words are Mayer, Davis and Schoorman's, and Appendix G says where to read them"*), and a line on the copyright page. **The Appendix G entry also marks what is not borrowed** — *"Chapter 1 adds the asymmetry between the three: the first two leave a record somebody can check, the third leaves none, so they guess. The paper does not say that; I do."* | Appendix G's promise — *"where an idea here is load-bearing, it has a lineage"* — holds for the one place a reader would most reasonably ask, and the reader gets the primary source rather than a half-remembered talk. | **APPLIED 2026-08-05.** `citation_audit` **0 uncredited, 0 dead**; the two standing BORROWED findings are the unchanged inheritance cases. Appendix G entry drafted at `copula 2.58 / be 1.31` against the comparable Meadows section's **0.86 / 1.17**, which said drift rather than register; rewritten on verbs to **be 0.36, copula 0.00, waste 0.16, head 0**. ch1's sentence `be 0.59, copula 0.57, head 0`. `shipcheck` SHIPPABLE, review seven of seven `ok`. **The instrument gap this exposes, now closed.** `citation_audit.py` could not see this and would not have seen the next one: its UNCREDITED check keys on **a name beside a claim verb**, and an uncredited term of art names nobody by definition. A borrowed idea is invisible to a name-based audit exactly when it is most uncredited. `benevolence` added to the BORROWED table, which is the half of the instrument built for terms rather than names. |
| **DL-60** | `manuscript/ch7.md` — four sites; `instruments/prose_diet.py` `REGISTERS` (question, unruled) | **`ch7 passive 1.40` is the book's oldest standing heavy and had never actually been ruled.** It has been reported in every book-wide pass for weeks and read as pre-recorded; the log carries no entry deciding it. | **26 sites read in full. Four hide a real doer; the other twenty-two are the chapter's subject.** ch7 is the Diplomat, and it runs 62 instances of *repair / rupture / wound / betrayal* — a chapter about things that happen to people measures as a chapter with a passive problem. Several sites are load-bearing in a way active voice destroys: *"a man who had never been told to stop and had no way to be told"* (the **absence of a teller** is the sentence); *"the specific pain of being damaged by someone you were in relationship with. Not by an enemy, not by a stranger, but by the person who was supposed to hold the field with you"* (the passive is what carries the three-beat *not by / not by / but by*); *"every term softened until nothing's actually been said"* (agentless because evasion has no agent); and *"I can be told what I cost and stay in the conversation"*, a named formula repeated verbatim at `:160` and `:804`. | **Wendell 2026-08-05: *"apply the four and log the register question."*** Applied: `were repaired → she repaired`, `before it has been cleaned → before cleaning it`, `before it is closed → before you close it`, `when the repair has been negotiated → when you have negotiated the repair`. **The register question goes to Wendell and stays open** — `REGISTERS` is a standing exemption and only he can grant one, exactly as with the em-dash budget. | The four sentences that were hiding a doer stop hiding one, and the number that remains has a reason recorded beside it instead of being re-litigated every pass. | **APPLIED 2026-08-05. `ch7 passive 1.40 → 1.31`, measured on a scratch copy before proposing rather than predicted** — my arithmetic had said 1.18, and proportional reasoning about this counter is wrong. **Still 0.01 over, and that is where it stops.** Getting under 1.30 from here means damaging sentences to win a hundredth, which is the counter driving the prose instead of the reverse. **The site worth the edit regardless of the ratio is `:67`.** The paragraph is restoring credit to a woman whose labor nobody saw — *"the shape of what she held together without ever naming that she held it"* — and the very next sentence, *"Ruptures that were repaired before anyone saw them"*, erased her as the agent. The passive was arguing against the passage it sat in. **OPEN QUESTION for Wendell: a `REGISTERS` entry for `ch7` on `passive` only.** Precedent is the Headmaster's letter on `be`/`copula` and Quill's ch5 on `zombie`/`be`/`expletive`, both his rulings on 2026-07-31. Until he rules, **1.31 is the recorded number and not a defect.** |
| **DL-61** | `specs/DECISION_LOG.md` rows A2, A5-orig, A7-orig, DL-11, DL-15, DL-20; `back_matter/glossary.md`; `instruments/index_build.py` | **Wendell 2026-08-05: *"let's work on the open issues."*** Seven rows read OPEN. **Six had already closed and nobody came back to mark them.** | **A2** — the character sheet, *"promised 8, delivered 2."* ch2 through ch8 now each carry *"Add a line to the sheet"* and each chains to the last (*under the daemon → under the channel you skip → under the line you have not drawn → under the inheritance → under the harm you keep fixing → under your walk-away price*), and ch9 collects: *"Chapter 1 told you this would happen and left a line open on your character sheet for it."* **A5-orig and A7-orig** — superseded rows left reading OPEN directly beside the rows retracting them. **DL-15** — closed by use; 79 rows and every ruling since 2026-07-29 argued against this file. **DL-20** — closed by DL-48, and `shipcheck` category 1 has read clear since. **DL-11** — the only one needing real work: `prose_diet` reports against the book's own baseline and structurally cannot re-test a claim made against an external control, so `COPULA_JUNE`, the regex that produced the original number, was run over all nine chapters. | **Closed on the measurement, not on a feeling.** DL-11: **copula/1k 61.4 → 46.2**, corrected measure 42.3, level with Elliott 40.9 and Chou 41.1. No longer 2.14× anything. **Recorded and deliberately not opened as new work:** the two sibling metrics in the same spec table never moved — sentences ≤6 words **19.4% → 18.6%** against *Igniting Joy*'s 4.2%, mean sentence length **16.0 → 15.8** against 18.8. The copula pass happened; the sentence-length pass never did, nobody ruled it, and the short declarative is arguably this book's voice. | The open list stops carrying six items that are done, so the two that are genuinely open can be seen. | **APPLIED 2026-08-05.** Six rows closed with evidence. `shipcheck` SHIPPABLE, review seven of seven `ok`. **A defect in my own work, found by doing this and fixed here.** `termdebt.py` reports `Vulnerable Child` NEVER DEFINED, which is a **third over-fire class for DL-29** — ch2:384 defines her in apposition (*"the Vulnerable Child, the player who should have been holding it the whole time"*) and the rule cannot see an appositive gloss, the same family as the bold-gloss blindness A5 already documented. But chasing it found something real: **neither the index nor the glossary carried her at all**, and my `Player, the` entry — written yesterday — said *"What is at the center of the Forest when no daemon has the joystick"*, an abstraction where ch2 has a person. *"The whole walk is reaching her: getting to the center where she is, and putting the joystick back in her hands."* Both fixed; index 87 → 88 entries. **Why `glossary_check` check 3 could not catch this:** it requires a glossary entry for every term **the index already knows**, so a term missing from both is invisible to it. The check is only as complete as `TERMS`, and that limit is now written where the next reader meets it. |
| **DL-62** | `manuscript/ch9.md:288`, `:296`, `:474`; `appendices/APPENDIX_A_FOUR_ALLYSHIP_DOMAINS.md`, `APPENDIX_B_QUESTS_CAMPAIGNS.md` (converted, then reverted) | **I reported `gate` as retired vocabulary surviving in two shipping appendices, converted all thirteen sites to `part`, and was wrong.** Wendell asked for the conversion on my report. | **`gate` is live canon.** `ch9:474`: *"Before you step into the game: a gate scan. **Seven daemons and the child at the center: eight gates, eight questions.**"* That reconciles exactly with ch2's *"Seven parts… underneath all of it, at the center, the youngest part of you still waits"*, and Appendix G credits the architecture — *"Big Mind gives the gates their structure."* The appendices were consistent with the book. **What DL-8 and `SPEC_PRINT_READINESS` retired was the eight-gate WALK** — *gate as one of eight threshold moments*, a walk removed from chapters 4–8 — **not the word.** I checked ch2's roster and the retired glossary and never checked ch9, then reported a defect on that basis. | **Reverted, both files, before the pass ran.** The check that caught it was reading `gate` in the **built** book rather than in the two files I had decided were the problem — the conversion was already applied when the build showed five surviving uses in ch9 and four in Appendix G. | The appendices keep the vocabulary the chapters use. | **REVERTED 2026-08-05, no net change to either appendix. The lesson, recorded because it cost a full conversion:** *a term is not retired because a retirement notice mentions it.* DL-8 retired a glossary that defined `Gate` wrongly; `SPEC_PRINT_READINESS` counted gate-walk references as evidence the appendices described a dead book. Both are true, and neither says the word is dead. **Checking the retirement notices is not the same as checking the book.** · **The real finding, and it is a ruling for Wendell: `gate` carries two incompatible senses inside ch9.** At `:288` it is the five channels — *"Fear, anger, sadness, joy, neutrality: each one a gate you can walk through instead of a threat to manage"* — and `:296` continues in that sense. At `:474`, 186 lines later, it is *"eight gates, eight questions."* A reader who takes the word at `:288` and meets it at `:474` has five gates and eight gates in one chapter. One sense keeps the word. |
| **DL-63** | `appendices/` — new component, NOT STARTED; `manuscript/ch1`–`ch8`, `ch9:682` | **Wendell 2026-08-05: *"make a note to create an example character sheet page in the appendix that readers can use to create their own in their notebooks as well as a link to a pdf of the character sheet."*** Recorded as a note, not built. | **The sheet's contents are already fully specified by the book**, which is what makes this buildable rather than a design problem. A2 (closed in DL-61) traced the chain: ch1 seats the myth, then seven lines accumulate, each one written *under* the last. **ch2** the daemon, under the myth · **ch3** the channel you skip · **ch4** the line you have not drawn · **ch5** the inheritance you carry · **ch6** the harm you keep fixing · **ch7** your walk-away price · **ch8** the game you reach for when you are past what you can hold. ch9 collects: *"Chapter 1 told you this would happen and left a line open on your character sheet for it. Fill it in now, with the Face and the number."* | **Wendell's ruling to make, not mine.** Open questions the note exists to hold: which appendix letter (the lettered set runs A–G and Appendix C's slot has already been re-seated once); whether the page prints as a filled example, a blank template, or both; and the PDF, which is the first component in the book that would route a reader off the page since DL-48 cut bars-engine — `shipcheck.py` category 1 exists to catch exactly that, so the link needs the same treatment `masteringallyship.com` got rather than a bare URL. | A reader who has been told eight times to add a line to a sheet can see the sheet. | **BUILT 2026-08-05 — see DL-67.** Was: NOTED, no work started, nothing on the spine. The eight lines above are the spec; whoever builds it should not re-derive them from the chapters. |
| **DL-64** | `manuscript/ch9.md:288`; the term `gate` book-wide | **DL-62 found `gate` carrying two incompatible senses inside ch9** — the five channels at `:288`, and *"eight gates, eight questions"* at `:474`, 186 lines apart. One sense had to keep the word. | **Every use in the book classified by context. The count is not close: roughly twenty sites in the daemon sense against one in the channel sense.** The daemon sense holds `ch9:474` (*"a gate scan. Seven daemons and the child at the center"*), the whole of Appendix A's affinity table, all thirteen quest and campaign headers in Appendix B (*"Chapter 4 · Gate: Skeptic"*), and Appendix G's section heading — **"The Shadow Work and the Gates"**, where the architecture is credited: *"Big Mind gives the gates their structure; Elliott gives them their spirit."* The channel sense is one sentence. | **`gate` = the eight interior figures — seven daemons and the child at the center. `ch9:288` gives up the word.** *"Fear, anger, sadness, joy, neutrality: each one a **gate** you can walk through instead of a threat to manage"* → *"each one a **road** you can walk instead of a threat to manage."* Chosen over *a way through the territory* (which repeats *territory* from the same sentence) and *somewhere to walk instead of something to manage* (vague on both halves). *Road* keeps the map frame the sentence has already set and keeps the walking verb that `:296` picks up in the next paragraph. | A reader meets one gate architecture, and the word points at what Appendix G credits it to. | **APPLIED 2026-08-05. One sentence. `gate` now reads 4 times in the chapters, all daemon sense.** `ch9` counters unchanged in band; `shipcheck` SHIPPABLE, review seven of seven `ok`. **The finding that made this a one-line fix rather than a rewrite:** `ch9:296` — *"The map shows you where the gates stand… the third time you arrive at the same gate and discover it was still on duty"* — reads as the **channel** sense only because `:288` primes it. On its own it is guard imagery, and a gate still on duty at the third meeting is exactly ch2's Protector holding one shape against a history you were not there for. **It was already the daemon sense; it was being misread by proximity.** Removing the competing use at `:288` fixed `:296` without touching it. **Measurement note, recorded because it nearly misled the choice:** the three candidate sentences were scored by `prose_diet` and returned `copula 3.66 / 1.83 / 3.97` — noise on a forty-word sample, exactly what the review skill warns about below ~300 words. The choice was made by reading and confirmed against ch9 whole. |
| **DL-65** | `specs/DECISION_LOG.md` rows A1 and A3; `manuscript/ch1.md:179`, `:207`, `:73`, `:83`; `ch2:292`, ch2 §7 | **Wendell 2026-08-05: *"let's do A1 and A3."*** Both were recorded 2026-07-31 as ch1 promising something the book does not deliver, and both offered the same two exits: narrow the claim, or seat the missing thing. **Neither exit was needed. Both findings were artifacts of how they were measured.** | **A1** — *Games of Chance / Skill / Passion*, 2,303 words of ch1, zero recurrence in ch2–ch9. **ch1:207 hands the taxonomy off inside ch1**: *"You just sorted your situation into three kinds of game. Now meet the six who can teach you to play it."* The three games are the board; the six Faces are how you play, and they carry the remaining eight chapters. The claim under suspicion — *"Everything ahead is built to strengthen your hand in all three: to keep you present for the breaks when they come, to sharpen the moves that are yours to make, and to deepen the passion that keeps you at the machine"* — is about the reader's hand, not about the words returning, and the book does all three. · **A3** — *"Allyship means helping the less powerful"*, the paternalism myth, *"named and never taken apart."* **The teardown is at `ch2:292`**: the Fixer as demon *"takes over, fixing people who never asked to be fixed"*. DL-56's Second Walk port strengthened it further — *"you arrive with a higher bar than Imani asked for, or a bar pointing somewhere she was not going… The Fixer three solutions deep does the same thing, arriving past a door that never opened."* | **Both closed as no-defect, on evidence rather than on a narrowed claim.** No prose changed. | The open list stops holding two items that would have cost either a weakened ch1 or a chapter's worth of new material to satisfy. | **CLOSED 2026-08-05, no edits. The measurement lesson, and it is the third time this session:** A3's original evidence was *"9 of 10 myths map to a teardown; this one returns 0 hits ch2–ch9."* Re-run, **three** myths return zero by phrase — *helping the less powerful*, *paying down what you owe*, *being seen doing it*. Tested by concept instead, two of those three are dismantled at length (*debt*, *applause / performing*), and the third is dismantled under the Fixer's name rather than the myth's. **A phrase count cannot see a teardown that never repeats the phrase**, which is the same defect as the hand-greps recorded earlier — a grep written to confirm is not a measurement. **The one cheap improvement available and deliberately not taken:** ch1:83 promises a diagnostic that *"hands each one back with the page that takes it apart"*, and nothing labels ch2's Fixer as the page answering the paternalism myth. A reader tracking her own myth makes that connection herself. That is polish rather than a defect, it is one clause if Wendell wants it, and inventing it unasked is how a closed finding becomes new prose nobody ruled. |
| **DL-66** | `instruments/prose_diet.py` `REGISTERS`; `manuscript/ch7.md` | **DL-60 left the ch7 `passive 1.31` register question open for Wendell.** *"Let's do the register question for ch7."* | **The two precedents decide it, and they point the other way.** `headmasters_letter.md` exempts a whole file because a personal letter is a different genre from expository prose. **`ch5.md` is not a chapter exemption at all** — it is a `blocks:` entry covering Quill's charter, 399 words inside a 10,000-word file, and the code comment beside it says a file-level exception would be wrong because it *"fires on the draft and never on ch5.md."* The table also carries a **RATCHET** rule in Wendell's own 2026-07-31 ruling: a ceiling is the measured value of the passage as it stands, it may be lowered when the prose improves, and **it may never be raised to make a batch pass.** So the question is not whether ch7 deserves a register. It is whether ch7's passive load sits in an identifiable block. | **Measured by splitting the chapter.** EA / Alchemy / TRANSLATE blocks, 3,904 words: **passive 1.13**, under the ceiling. Marginalia, 698 words: **0.92**. Expository prose, 10,630 words: **1.37** — *higher than the whole chapter's 1.31.* **The load is in the ordinary prose, and the framed blocks are cleaner than the chapter around them.** | **DECLINED.** No block register is available, because no block carries the load. A chapter-level register would exempt ordinary expository prose from the expository baseline, which is circular — the baseline is nine chapters of exactly this register. `HEAD_VOICE_DIAL`'s question is *is this the voice or is this drift*, and the skill's own default answer is drift; the twenty-two load-bearing sites in DL-60 are the reason the number is 1.31 rather than 1.15, not a reason to move the ceiling. | The exemption list stays a list of genres, not a list of chapters that ran heavy. | **RULED 2026-08-05, no change to `REGISTERS` and no change to ch7. `ch7 passive 1.31` stands as a reported number with a recorded reason.** `prose_diet` marks it LOOK rather than a failure, and DL-60 already holds the twenty-two sites and why each one keeps its passive, so a future pass reads the entry instead of reopening the chapter. **Reopenable on new evidence, and there is one thing that would reopen it:** if ch7's expository prose is ever rewritten and the number does not move, the load is structural to the chapter's subject rather than to its sentences, and that is a different argument from the one declined here. **The measurement that mattered was the split.** Reading the site list, the passives look like they cluster in the alchemy blocks — sixteen of them, all *"the wound has been cleaned"* and *"the repair has been negotiated"*. They do not. Counting them by block says the opposite of what reading them suggested. |
| **DL-67** | `appendices/APPENDIX_H_CHARACTER_SHEET.md` (new); `build_book.py` spine; closes DL-63 | **The book tells a reader to add a line to the sheet nine times and never shows her one.** ch1 opens it with four lines, ch2 through ch8 each add one, ch9 collects. | **Assembled from the sheet's own instructions rather than designed**, which is what made it buildable: every line is traceable to the chapter that asks for it, and the count came out at **thirteen** — four written in ch1, **two the book deliberately leaves open** (*"It fills in as you play: a superpower you will only spot in motion, an autopilot pattern you will catch yourself running"*), and one added in each of ch2–ch8, with ch9 filling the autopilot line. **A back-reference checked and found sound:** ch9 claims *"Chapter 1 told you this would happen and left a line open on your character sheet for it."* ch1 does, in that same sentence. It nearly read as a dangling pointer of the DL-53 class and is not one. | **Wendell 2026-08-05: *"let's do the character sheet page."*** Appendix H, three parts: the thirteen lines with the chapter that fills each; a worked example; and the blank, which is an instruction rather than a form, because ch1 rules pen and paper. The example is **Devon's**, a name unused elsewhere in the book, on the convention ch2 sets with Imani and ch3 with Dana. Its closing note is the design rule the sheet runs on: *"A name in them, or a number, or a sentence somebody could disagree with. I should be more assertive is not a line. To Priya: that I will stop covering is."* | A reader who has been told nine times to add a line can see a filled sheet and rule up her own. | **APPLIED 2026-08-05.** 845 words, on the spine as **Appendix H, GAP** — the instructions live in the chapters, so a reader can build one without this page; she should not have to. Build **128,417 → 129,261**; `shipcheck` SHIPPABLE, review seven of seven `ok`, `gate` and `voice` clean, `be 0.39, copula 0.29, waste 0.74, passive 0.00`. **Two defects in my own draft, both caught on the second read and both worth recording.** The worked example is first person throughout — *Channel I skip*, *Line I have not drawn* — and two lines slipped out of it: the daemon's job read *"absorbing the friction **himself**"*, and the superpower read *"a house where **you** found out how the evening would go by listening to a door."* **The second is `MANUSCRIPT_FILE_CANON:154` in a worked example** — a *you* inside somebody else's sheet narrates the reader's childhood back to her as fact, which is the one thing that rule exists to stop, and no counter can see it. **The PDF is pointed at `masteringallyship.com` and no deeper**, matching `ch1:269`. A path invented here would be a placeholder wearing a URL's clothes, and `shipcheck` category 2 exists because those typeset verbatim. The exact address is Wendell's to set. |
| **DL-68** | `appendices/APPENDIX_H_CHARACTER_SHEET.md` (rewritten); `manuscript/ch1.md` Build Your Allyship Character; `instruments/character_sheet.html` (new) | **DL-67's Appendix H listed the sheet's fields instead of being the sheet, and the book never pointed at it.** Wendell, on the first layout: *"the way they are showing up doesn't make sense… there are duplications and things that don't really show up."* | **Three defects, all from building on a 5E skeleton instead of the book's.** (1) **Six ability-score boxes for the Faces were invented mechanics** — no chapter asks a reader to rank the Faces; `ch1` asks for exactly one, *"the one you already play."* (2) **Home face appeared twice**, in the header band and again in the Face block. (3) **The superpower was stamped to a chapter and has none** — `ch2:342` defines it and never says to write it down, and `ch1` says the sheet *"fills in as you play: a superpower you will only spot in motion."* | **Wendell 2026-08-05: the sheet fills in the order the material shows up in the book.** So the layout is a **track**, not a stat block: every field carries the chapter stamp that asks for it, ch1's four lines first, then one per chapter through ch9, and the superpower stamped **OPEN**. The six Faces became a pick-one ring row rather than six ranks. **No School field**, by his ruling — it is derivable from the home face. Appendix H now **shows** the sheet, with the worked example kept beneath it, and `ch1` introduces it: *"Every roleplaying game hands you a sheet before it hands you a story… Allyship gets played without one. You can be held to a sentence you wrote. You cannot be held to one you only meant."* | The sheet is the thing the book asks for, in the order it asks, and the chapter that introduces it says where to find it. | **APPLIED 2026-08-05.** Appendix H **845 → 891 words**; build **129,261 → 129,405**; `shipcheck` SHIPPABLE, review seven of seven `ok`. ch1 insert measured `be 0.93, copula 0.43, waste 0.83, passive 0.00, empty 0.00, head 0`; ch1 whole unchanged in band. `instruments/character_sheet.html` is the print and screen source, kept in the repo so the artwork and the appendix cannot drift apart. **The print constraint, ruled by Wendell and worth recording because it nearly shipped wrong:** the book prints black and white, and the five channel swatches carry the wu xing colours. **Converted to greyscale, Wood `#3F6B3A` and Fire `#A33427` land six points apart and read as one tone** — three of the five would have collapsed. Print now restates them from the tradition's own values (**Metal is white, Water is black**, the middle three separated across the range), screen keeps the colours, and the swatches were moved from inline styles onto tokenised classes so the two can differ without touching the markup. **The swatch never carries alone** — every row reads *Metal · fear* in text — which is why keeping colour for the fillable version costs the printed sheet nothing. |
| **DL-69** | `instruments/character_sheet.html`; `instruments/sheet_check.py` (new) | **Wendell 2026-08-05: *"keep the html in the repo."*** DL-68's commit message justified keeping it there by claiming it *"stops the artwork and the appendix from drifting apart."* **That was wrong on its own terms.** | **Proximity is not a check.** The sheet now exists in three places — the HTML artwork, Appendix H, and the chapters that ask for each line — and nothing read more than one of them. This session has watched the glossary, the index, the enrollment page and Appendix H itself each drift from the book within a day of being written; every one was caught by an instrument that read two things, and never by two files sitting in the same directory. | **`sheet_check.py`, three comparisons.** (1) Same fields, same order, HTML against Appendix H. (2) Same chapter stamps. (3) **Every stamp answers to the book** — a `CH n` stamp claims chapter n asks the reader to add that line, so the check greps chapter n for the instruction. `OPEN` is exempt and required to stay exempt: the superpower has no chapter by design, and a `CH 2` stamp on it would be the defect. | The three copies cannot disagree without something saying so. | **APPLIED 2026-08-05.** Reports 13 fields in agreement, exit 0. **Tested by breaking it three ways**, because a check that has never failed is a check nobody has tested: renaming a field in the appendix → *"FIELD differs at CH 7: html 'walk-away price', appendix 'exit price'"*; moving a stamp in the HTML → the full stamp sequences printed side by side with the mismatch visible; and cutting *"Add a line to the sheet"* out of ch6 → *"STAMP CH 6 does not answer to the book."* All three restored, `git status` clean. **Check 3 is the one that matters, and it is the check the first sheet needed and did not have.** DL-68's six ability-score boxes were an instruction no chapter ever gave, and nothing in the repo could have said so — a field with an invented stamp reads exactly like a field with a real one. |
| **DL-70** | `instruments/termdebt.py`; closes DL-29 | **`termdebt.py` reported five NEVER DEFINED rows and every one was wrong.** DL-29 opened on 2026-08-01 after the rule had already cost two withdrawn findings; a third, `Vulnerable Child` again, turned up 2026-08-05 chasing the open list. | **Three over-fire classes, and the third was hiding inside the other two.** (1) **Gloss inside the bold span** — `is_def` matched a dash *after* the closing asterisks and missed `**Direct Action — the true thing said to the face**`. All four domains are defined that way, and all four read as undefined for months; this is the miss that withdrew A5. (2) **The appositive** — *"the youngest part of you still waits: the Vulnerable Child, the player who should have been holding it"* and *"one of the four: Direct Action, the true thing said to a face."* DL-29 named this shape and it was still unimplemented. (3) **Four rows for terms the book does not contain.** `Amber`, `Orange`, `Green`, `Teal` were CUT by Wendell on 2026-07-31 (row A4) and left in `TERMS`, so the ledger reported NEVER DEFINED for words that appear **zero** times. | **Wendell 2026-08-05: *"let's do the termdebt over-fires."*** Two new shapes added, the four dead rows removed rather than commented out — a commented row is the next version of the same mistake. | The ledger reports term debt instead of reporting its own blind spots, and the next real finding will not be withdrawn. | **APPLIED 2026-08-05. Five NEVER DEFINED → zero, no manuscript change.** **The appositive rule over-fired on its first draft and the catch is the point.** Written loosely as *TERM, the …*, it matched `ch6:242` — *"At the Architect's altitude, the native material is not emotion"* — where the comma ends a fronted adverbial and nothing is defined. **That is the worse failure direction: a missing definition gets reported, a fabricated one is silent.** The discriminator turned out to be in the book's own habit — every true appositive definition here **announces itself with a colon** (*"That conversion has a name: Emotional Alchemy, the engine under the whole book"*), so the rule now requires one. `altitude` went back to its real state: first used `ch3:241`, defined `ch7:89`, **four chapters of debt**, which is a genuine finding the instrument exists to make and is left standing for a prose pass. **DL-29's second half was already closed** — its `citation_audit` DEAD complaint was fixed by the `EXEMPT_DEAD` block and the file-scope change of DL-50; DEAD has read 0 since. |
| **DL-71** | `specs/BOOK_BRIEF.md` (new); `SPEC_EDITORIAL_OS_INTEGRATION_2026-07-31.md` §4; closes DL-13 | **The brief existed and was never confirmed.** DL-13 has read *"OPEN, awaiting Wendell"* since 2026-07-31, and its decision column asks to *"confirm or amend the assembled book brief"* — not to write one. A draft sat inside §4 of an integration spec, which is the complaint the row itself makes: *"existed in pieces, never on one page."* | **Wendell 2026-08-05: *"let's do DL-13."*** Confirmed against the current book rather than re-read as written, because it was assembled a week and roughly 20,000 words ago. Moved to its own file. | **Three amendments.** (1) **The voice count was wrong.** The draft said *"seven in-world voices with ruled genres."* `HEAD_VOICE_DIAL` rules **six**, one per school; the seventh voice is the Headmaster, whose genre it does not cover. That spec's own **§2a, "the seventh dial"** — person, and feeling against fact — is a different thing and the likeliest source of the miscount. (2) **One boundary verified rather than asserted:** *no Head names a channel* now measures **0 hits** across nine chapters and the letter — no `Metal`, `Water`, `Wood`, `Fire`, `Earth` inside any marginalia, handbook or epigraph block. (3) **One betrayal is live and stays listed with its number:** *the short-declarative drift* was ruled drift on 2026-07-29 and has not moved — sentences ≤6 words at **18.6%** against *Igniting Joy*'s 4.2%, against 19.4% when the rule was made. | Every editorial pass judges against one page, and the page can be checked. | **CLOSED 2026-08-05.** **I was wrong twice before doing this and the correction is the useful part.** I told Wendell in two consecutive turns that the `mtgoa-review` skill had probably become the brief. **It disclaims that role in its own closing section** — *"`SPEC_EXAMPLES`, `SPEC_TWO_HANDS`, `SPEC_FACE_TARGETS` and `HEAD_VOICE_DIAL` say what the prose has to do. This pass only says whether it is heavy, off-gate, or across the membrane. A passage can pass every check here and still be the wrong paragraph."* A process that measures prose is not a brief that says what the prose is for, and reading the row's own decision column would have shown that a brief already existed and needed confirming rather than replacing. **The live betrayal is named on the page on purpose:** a brief listing only the failures the book avoids is a brief nobody has to check anything against. |
| **DL-72** | `manuscript/ch3.md:241` | **`altitude` was used 45 times and first explained four chapters after first use** — the one live term debt left standing by DL-70, and the finding the termdebt repair was built to make. | **The reader meets it cold and in a demonstrative.** `altitude` appears **0 times in ch1 and ch2**, and the concept is not taught earlier under another name — no *developmental level*, no *the levels*, no *six levels*. Its first appearance, `ch3:241`, is body prose that says **"This altitude"** three times in one paragraph, a demonstrative pointing at something never established. It was not explained until `ch7:89`, *"The Diplomat's altitude is the one at which a person can hold multiple valid perspectives simultaneously."* The paragraph itself is doing real work — it argues that the reader's own level sanctions Feeling in its group form and warns against both private Feeling and Function — and needed only the term. | **Wendell 2026-08-05: *"let's do the altitude debt."*** A gloss at first use, the same remedy `ch6:238` uses for `leverage point`: *"Your altitude is the level you play from, and every altitude permits some moves and warns you off others. This one fully sanctions Feeling in its collective form…"* The demonstrative becomes *This one*, so the paragraph's later *"this altitude issues them loudest"* lands on a term the reader now holds. | The book's second-most-used piece of vocabulary is defined where a reader first meets it. | **REVERTED 2026-08-05, same day — see DL-73. The finding was wrong and the fix broke the book's structure.** Everything below stands as the record of a mistake. `termdebt`: `altitude ch3:241 → ch3:241`, debt cleared, no chapter gap. Section scope `copula 1.10 → 1.19` in band, `zombie 1.44 → 1.38`, `empty 1.89 → 1.81`, everything else flat; both heavies pre-existing and both improved. Build 129,405 → 129,424; `shipcheck` SHIPPABLE, review seven of seven `ok`. **The first draft did not register and the reason is a real limitation, recorded rather than patched.** I opened the sentence *"Altitude is the level you play from"* — capitalised, sentence-initial — and `termdebt` still reported the debt, because `TERMS` matches `\baltitude\b` **case-sensitively** and `is_def` inherits that. The case sensitivity is deliberate and must stay: `Fire` the channel and `fire` the verb are different terms, and making `is_def` case-insensitive would hand the five channels a flood of false definitions. So **a definition that opens a sentence with its own term is invisible to this instrument**, which is a live under-fire class for any future gloss. Fixed in the prose instead — *"Your altitude is…"* — which is better anyway, since it ties the level to the reader rather than announcing a term. **What is left and deliberately not chased:** six smaller debts, largest two chapters (`quest` ch1:239 → ch3:966), and at least one of them looks like a further under-fire rather than a real gap — `ch1:239` defines *quest* by contrast, *"Not 'get better at allyship.' A cause,"* a shape `is_def` has no rule for. |
| **DL-73** | `manuscript/ch3.md:241` (reverted); `instruments/termdebt.py` `WITHHELD`; reverses DL-72 | **DL-72 glossed `altitude` at first use to clear a four-chapter term debt. The debt was the design.** | **Wendell 2026-08-05: *"the Faces are all at different altitudes. This is a bit of an 11th hour reveal for the reader. They've been going through a developmental process and finding out after they've done it that's what's been going on (specifically to deal with green's hierarchy allergy). This is why altitude hasn't shown up before this point."*** **The book bears him out and the evidence was in front of me.** Usage by chapter: **0, 0, 4, 6, 2, 7, 14, 30, 3** — a ramp that peaks in the Sage. The reveal is `ch8:264`: *"**Which altitude is this?** is a vertical question, and its answer-set is the six roles the schools teach. It asks what a person can currently hold: the size of the frame they operate from, **a fact about their development and not a fact about their worth.**"* `ch8:254` supplies transcend-and-include immediately before it; `ch8:270` guards the misuse — *"the game read gets converted into an altitude verdict."* The whole reveal ships with its own antidote attached, which is what makes holding it safe. | **Reverted.** `ch3:241` returns to *"This altitude fully sanctions Feeling in its collective form."* An unglossed demonstrative is what lets a reader pass over the word; **"Your altitude is the level you play from" tells her in Chapter 3 that she occupies a rung**, which is the exact rejection the structure is built to avoid. And it landed in the Shaman chapter, the reader's entry point. | The reader walks the developmental sequence and is told what it was after she has walked it. | **REVERTED 2026-08-05, and the ruling encoded rather than written down.** `termdebt.py` gains a **`WITHHELD`** map: terms whose gap is design, reported as `WITHHELD BY DESIGN` and excluded from the debt list, each carrying its reason and its reveal location. Build back to 129,405; `shipcheck` SHIPPABLE, review seven of seven `ok`. **What I should have caught before editing.** The distribution was on my screen — I printed `ch1 0, ch2 0` and read it as *the reader meets it cold* rather than as *the book is holding it.* **A term that is absent from the first two chapters and peaks in the eighth is not a term somebody forgot to define.** Every other repair this session tested a claim against the book; here I tested only whether a definition existed, never whether its absence was doing work. **The instrument cannot tell a withheld term from an undefined one**, which is the same failure family as DL-62's `gate` — a retirement notice is not the book, and a term-debt row is not intent. It is the fifth time this session the fix has been to move a ruling into the file the instrument reads. |
| **DL-74** | `instruments/termdebt.py` `WITHHELD` and known-limits block | **Wendell 2026-08-05, after DL-73: *"let's check the other six term debts for design."*** Thirteen rows, not six — my count came off a truncated list. Every one read. | **One is design, two are the instrument under-firing, ten are benign.** · **DESIGN — `superpower`.** `ch1:241` withholds it in the same sentence that withholds the autopilot line: *"a superpower you will only spot in motion."* `ch2:344` says what one **is** and never tells the reader to write hers down. This was already ruled twice without being connected to the ledger — Appendix H stamps that field **OPEN** rather than to a chapter (DL-67, DL-68). Same shape as `altitude`: the gap is the teaching. · **UNDER-FIRE — `quest`.** `ch1:239` defines it by contrast: *"**Your quest.** Not 'get better at allyship.' A cause. The specific fight, community, or person you are actually in this for."* `is_def` has no rule for *Not X. A Y.* · **UNDER-FIRE — `Player`.** Defined twice in ch2, `:384` *"the player who should have been holding it the whole time"* and `:429` *"a player is waiting for somebody to show up and play"* — **both lowercase**, while `TERMS` matches `\bPlayer\b`. The case sensitivity is deliberate and must stay (`Fire` the channel against `fire` the verb), so the ledger reports first use at `ch8:348` and cannot see either definition. | **`superpower` added to `WITHHELD` with its reason. Neither under-fire gets a new rule.** This session already watched a loosely-written rule manufacture a definition at `ch6:242`, and **a fabricated definition is silent where a missing one at least gets reported.** Both are recorded in the file instead, with the text that proves them. | The ledger's remaining rows are known quantities rather than a list nobody has read. | **APPLIED 2026-08-05, no manuscript change. Debts 13 → 12 reported, 2 now `WITHHELD BY DESIGN`.** The ten benign rows were read and are recorded in the file so the next pass does not re-derive them: the five channels are named at `ch3:435` and tabled at `ch3:445–449`, one passage; `Shaman` and `Diplomat` are mentioned at `ch1:121`/`ch1:143` and rostered at `ch1:209`, with `ch1:121` signposting *"the Shaman in Chapter 3"*; `Game Master` is the book's opening claim and unpacked in its own paragraph; `the Forest` enters as an invocation in the Reader's Oath and is the subject of ch2 §3; `the joystick` is planted at `ch1:62` and ch2 §6 is titled after it. **The finding worth carrying forward:** `superpower` had been ruled design twice this week, in the character sheet work, and the ledger still called it a debt — **because the ruling lived in an appendix and a log row rather than in the instrument.** DL-73 was the fifth instance of that pattern; this is the sixth, and it was sitting in the same file I had just edited. |
| **DL-75** | `instruments/review.py` draft mode and `book()`; `instruments/voice_surfaces.py` (new); `appendices/APPENDIX_H_CHARACTER_SHEET.md`; `appendices/ON_THE_SHOULDERS_OF.md` | **Wendell 2026-08-05: *"let's do a full book-wide review pass."*** The board was clean and the pass's own step 8 — the slop reading — had never been run on most of what landed today. | **The slop reading found one recurring pattern in my prose and two instrument defects underneath it.** · **The pattern: the explanatory colon.** Not the banned colon reveal, its quieter cousin — clause, colon, restatement. Two were restatement rather than enumeration and both are fixed: `APPENDIX_H` *"…and it is right: a sheet you can only reach through a screen"* and `ON_THE_SHOULDERS_OF` *"the asymmetry between the three: the first two leave a record."* The glossary's eleven are list colons, which the rule permits. · **Defect 1 — draft mode ignored every ruled exemption.** `review.py` imported `COUNTERS` from `gate.py` and applied them raw, without `exempt_spans`, so running it on Appendix G reported `banned rooms` — which Wendell **ruled keepable on 2026-07-30**, *"we can leave rooms in this example. It's not load bearing."* `gate.py` honoured the ruling; the draft path re-reported it, which invites the next pass to fix prose the author kept. · **Defect 2 — the voice linter had never read the appendices.** `marginalia/review.py`'s `path` defaults to `manuscript/` and `book()` called it **with no arguments**, so from the day that step existed it covered nine chapters while `gate.py` covered four surfaces. | **Both fixed and the coverage gap closed.** `exempt_spans` now applies in draft mode. `voice_surfaces.py` walks the spine's non-chapter surfaces one at a time, because the linter takes a single path, and is wired in as step **0b**. | The pass reads what the book ships, and a ruling made once is honoured on every path. | **APPLIED 2026-08-05. Step 0b reports `TOTAL BLOCK 4  WARN 24` across 18 shipping surfaces that had never been linted for voice** — Appendix C 1, Appendix E 2, Appendix G 1. **Left for Wendell, not fixed:** Appendix G's is *"the problem isn't the practitioner. It's the design of the game"* — a denying negation by the linter's rule, and **the book's central empirical claim**. Rewriting a thesis to satisfy a counter is his call. Today's new files came back clean: Appendix H **0 BLOCK**, the glossary **0 BLOCK**. **Two of my own errors caught while fixing this, both recorded in the files.** I first wired step 0b to pass many paths to a linter that takes one — and **the step printed `ok` above the words "error: unrecognized arguments"**, because the runner's `or "review.py" in cmd[0]` fallback swallowed it. That is DL-35 exactly: a step that cannot fail. The runner now fails on `error:` or `Traceback`. And `voice_surfaces.py`'s first draft labelled six **clean** files *"unreadable"*, because the linter prints *"clean — no findings"* instead of a summary line — the same mislabelling `glossary_check.py` shipped this morning. |
