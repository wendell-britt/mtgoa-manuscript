# slop_shapes drawn to zero

**2026-09-12.** slop_shapes 40 unresolved → **0**. Wendell's ruling: rewrite the BANNED, the
FAUXINSIGHT, and the denying "not X, but Y" — including `not a catastrophic violation, but a real
one` and the three Skeptic-flagged borderlines — and keep only the genuine concessions and
rankings. **36 rewritten, 1 boxed note, 4 kept (ratified).**

**No scanner rose at any line.** slop_shapes 47 → 12 total; trailing_and, telling, fragment,
light_verb unchanged per file:line. gate 0, claims pass, round-trip byte-identical, no new voice
BLOCK.

## The moves

- **BANNED → the plain word (5 rewrites):** `leverage` → *hold* / *the upper hand* / *a threat*;
  `facilitated conversation` → *someone to run the conversation* / *the managed conversation*.
- **FAUXINSIGHT → the claim stands alone (8 rewrites):** the lone-expert framing removed —
  `what nobody has named` → *what goes unnamed*; `Nobody gets wronged in a way anyone can name` →
  *The wrong never takes a nameable shape*; `Nobody tells you that.` deleted; `to be the one brave
  enough to have said it` → *for the credit of having said it*; and the rest.
- **BINARY denying → state Y directly (23 rewrites):** the `not because A, but because B` scaffold
  dropped, leading with the real reason (`gets the most airtime because pain has become the
  credential`); `not X, but Y` collapsed to Y (`by ruling that the inheritance is not yet in a
  condition to be inherited`). You re-add any negation you want back.

## Kept (5)

- **Boxed:** `ch7:21` — the applicant's quoted `I had no leverage…` is inside the ch7 HANDBOOK box;
  edited separately, so a ledger note (checked boxed status up front this time).
- **Four genuine contrasts (ratified):** `It is not pleasant, but it is fun.` (concession);
  `not as a clean line, but as an explosion` (two real modes); `not just from the villagers, but
  from their children` (additive — *not just X but also Y*); `not from after it, not from analysis,
  but from the felt sense` (three-vantage ranking).

Plus the 7 ranking/definitional entries already accepted in the 2026-09-09 intake, untouched.

## One re-key

The `ch9:409` rewrite dropped a `not because…but` opener, which changed the folded key of a
polysyndeton serial-list entry (`the map and the willingness to walk it and report back`) that the
sentence still contains. Pruned the stale entry, re-accepted the current one.

## Board

slop_shapes 0 · trailing_and 0 · polysyndeton 0 · fragment 0 · light_verb 0 · gate 0. **Five of six
at zero.** Remaining: **telling 195** — the last, and the biggest.
