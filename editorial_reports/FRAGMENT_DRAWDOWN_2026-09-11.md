# Fragment drawn to zero

**2026-09-11**, on `origin/master` after the v33 sync. **fragment 54 → 0 unresolved.** One prose
rewrite; the rest resolved by ledger notes and one corpus exclusion. No other scanner rose at any
line from the rewrite; the ledger is clean (stale=0).

The v33 splitter fix is what made this board honest: bold headers stopped false-flagging, and real
fragments hidden behind emphasis surfaced.

## How the 54 resolved

| count | disposition | what |
|---|---|---|
| 8 | **ledger — boxed note** (your ruling) | HANDBOOK records v33 revealed (7 charter clauses + a Head's word); scored, edited separately, per the boxed-records ruling |
| 1 | ledger — boxed note | `Any of it.` (ch8 admissions box) |
| 4 | **corpus exclude** | the glossary joined the index as apparatus — reference entries terse by convention, read as 4 fragments (and 3 copula-labels; telling 198 → 195) |
| 1 | **rewrite** | ch3:775 — a series item split off by a stray period, rejoined: `…fear rather than concern, sadness rather than disappointment.` |
| 16 | **ledger — deliberate beat** (my call) | the book's verbless voice beats (`Years of them.`, `Endless revisions.`, `Small ones first.`) — the same style as the voice anchors |
| 8 | ledger — list lead-in (my call) | a label before the list it introduces (`The Skeptic's five:`) |
| 4 | ledger — drill spec (my call) | terse practice parameters; one entry covers all six `Three exchanges at minimum.` |
| 2 | ledger — apparatus (my call) | a source-attribution line and a worksheet checkbox row |
| 2 | **ledger — negation, FLAGGED** (my call) | `Not this one, not yet, not from me.` and the ch6 not-X-but-Y `unnecessary` clarification — you rewrite negations yourself, so these may become rewrites |
| 1 | prune | the 1 stale boxed entry from step 2 (`Entry.** None.`), text shifted under v33 |

**Ledgered, not accepted:** 32 of these rest on **my call and need your ratification** (the beats,
lead-ins, specs, apparatus, negations). 9 rest on your prior boxed-records ruling. The 2 flagged
negations are the ones most likely to want a rewrite instead.

## Also cleaned: 2 stale trailing_and entries

v33's re-splitting resolved two serial-list `trailing_and` hits (`A verb, a name, a deadline, and
an artifact…`, `One per Face, each with an admissions page…`) — they no longer fire as loose tails
under the corrected boundaries. Their now-dead ledger entries were pruned (verified: neither fires
as a hit any more, accepted or not). trailing_and 88 → 86, stale 0.

## Board now

fragment 0 · trailing_and 0 · polysyndeton 0 · gate 0 · claims/round-trip/seam pass. Remaining
zero-target debt: **telling 195, light_verb 52, slop_shapes 40 = 287.**
